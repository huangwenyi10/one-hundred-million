---
name: one-hundred-million
description: 3~5 年挣 1 亿的个人战略总技能：目标路径框架（衔接 one-hundred-million-path-review）+ 短视频制作子模块（口播稿+视频+配音+字幕+水印+封面，覆盖架构师/大数据/AI/前端/产品/测试/管理/软技能等训练营与知识类短视频，抖音/快手/小红书/公众号/YouTube 等平台）。触发词：1亿目标、财富目标、短视频制作、训练营视频、口播稿、技术短视频、生成视频并配音配字幕。
agent_created: true
disable-model-invocation: false
---
# One Hundred Million（3~5 年挣 1 亿 · 个人战略总技能）

## 一、目标总纲

- **总目标**：3~5 年挣 1 亿人民币。作者：Map，计算机专业 10+ 年行业经验，可承担技术合伙人/创始人角色。
- **底层逻辑（诚实版）**：1 亿无法靠工资、课时费、接单实现，只能靠股权、资本等高杠杆；而个人影响力的建立是撬动杠杆的前提。短视频与知识内容不是终点，而是「内容获客 → 建立信任 → 高客单服务/股权机会」漏斗的第一环。
- **子技能地图**：

  | 子技能 | 职责 | 位置 |
  |--------|------|------|
  | A 短视频制作 | 内容获客与影响力杠杆：口播稿+视频+配音+字幕+水印+封面全流程（当前最成熟执行模块） | 本技能「三」 |
  | B 路径规划/季度复盘/决策审查 | 1 亿路径的数学校验、90 天检查、关键决策（换方向/融资/退出）审查 | 独立技能 `one-hundred-million-path-review` |
  | C 预留 | 产品化/资本化/团队扩张等后续模块 | 未成型前不写死，成型后补入本技能 |

- **总技能纪律**（与 `one-hundred-million-path-review` 保持一致）：
  1. 诚实优先：数学上不成立的方向直接指出，不做心理按摩。
  2. 纸面财富 ≠ 落袋：区分估值持股与可变现资产，锁定期/稀释/对赌都要计入。
  3. 止损纪律：每个方向预设 kill criteria，触发即换，不恋战。
  4. 内容忠于事实：所有产出物（口播稿、画面、数据、结论）禁止编造——1 亿目标的内容更不允许用虚假案例撑场面；正确性优先于完整性，拿不准的略过或留白。
- **触发路由**：用户提「1亿目标 / 财富复盘 / 换方向 / 融资节奏 / 退出时机」→ 主走子技能 B（加载 `one-hundred-million-path-review` 执行），本技能提供总纲上下文；用户提「做视频 / 口播稿 / 训练营 / 短视频 / 配音配字幕」→ 主走子技能 A（下方完整流程）。
- **短视频在总目标中的战略定位**：账号内容围绕作者可交付的高客单服务（各训练营等）获客——前 3 秒钩子、CTA 不只是涨粉，而是筛选出有付费/合作意愿的观众；每期内容数据（metrics.csv）反哺选题，并记录「线索 → 转化」情况，作为 1 亿漏斗的一环；作者需要整体路径规划/复盘时切子技能 B。

## 二、实现方案与步骤（3~5 年路线）

### 1. 数学基础（不可绕过）

- 目标：3~5 年个人可变现净资产 ≥ 1 亿人民币。
- 工资、课时费、接单等线性收入路径不可达（线性收入 × 年限与 1 亿差一个数量级）。
- 唯一机制：**股权杠杆**——持有高比例股权的公司达到 4~7 亿估值并退出；**资本杠杆**（高倍率交易）风险极高，仅辅助、不作计划内路径。
- 兑现公式：落袋 = Σ(持股比例 × 退出估值 × 变现折扣)，稀释、锁定期、对赌全部计入。本方案与 `one-hundred-million-path-review/references/roadmap.md` 的四条路径框架一致，动态校准走子技能 B，不重复建框架。

### 2. 作者杠杆盘点（基于背景，诚实列出已有与缺口）

| 已有（可用杠杆） | 缺口（需补齐） |
|---|---|
| 计算机 10+ 年技术背景，可承担技术合伙人/创始人 | 公司/股权载体（目前没有） |
| 成熟内容管线：口播稿+视频+配音+字幕+水印+封面全流程 | 规模化现金流（未验证） |
| 账号 IP「Map 架构师训练营」+ 8 类训练营产品矩阵 | 行业标杆案例（信任背书） |
| 选题/数据反哺闭环（metrics.csv + 选题引擎） | 高客单企业端转化通道 |

### 3. 路径适配（四条路径 × 作者优势）

- **主路径（路径 A/B 结合）：内容 → 影响力 → 技术合伙人/创始人**。用内容建立的技术 IP 作筹码：要么被 pre-A 轮、有独角兽潜质的团队邀请任 CTO/联创拿 5-15% 股权（路径 B，投前尽调创始人品性/股权结构/赛道天花板）；要么自建公司，以技术能力 + 自带获客能力创业（路径 A，持股 15-25%）。**短视频内容不是收入来源，是换取股权筹码的信任资产**——这正是作者相对普通创业者的独特杠杆。
- **保底路径（路径 C）：高客单知识服务 + 出海 SaaS/独立产品**。训练营升级为企业内训/咨询/陪跑，提供现金流与标杆案例；出海 SaaS 目标 1000万~3000万，失败成本最低，可作主路径的前置或并行弹药池。
- **辅助路径（路径 D）：交易/加密杠杆仅限纪律约束**：只用主路径利润的闲置部分，任何时点回撤 >30% 强制降杠杆；其成功概率 <10%，不进计划、不占用本金。

### 4. 分阶段步骤（3~5 年，阶段可并行）

| 阶段 | 时间窗 | 核心目标 | 关键动作 | 检验点 / kill criteria |
|---|---|---|---|---|
| 0 蓄水 | 0-6 个月 | 影响力规模化 + 产品化 | 账号矩阵按 70%S/20%M/10%L 起量；训练营课程产品化；搭建高客单转化漏斗（CTA 引导私域/咨询，不只为涨粉） | 单平台稳定出爆款；出现付费/咨询线索；每 90 天检查 |
| 1 验证 | 6-18 个月 | 高客单跑通 / 股权机会落地 | 企业内训/咨询/陪跑成交数个标杆客户；或加入早期团队任技术合伙人（尽调：创始人品性/股权结构/赛道天花板，TAM <30 亿直接放弃） | 高客单复购或转介绍；若合伙则 5-15% 股权落纸；最多试 3 个方向 |
| 2 放大 | 18-36 个月 | 现金流规模化 + 杠杆切换 | 服务/产品收入增长（参照 roadmap 第 2 年指标：获客成本回收期 <12 个月）；自建公司则启动融资，融资节奏跑在现金流断裂前 12 个月 | NRR ≥120%；连续 2 个季度不达即换向 |
| 3 退出 | 36-60 个月 | 股权价值兑现 | 公司估值向 4-7 亿推进；启动 IPO/并购退出；锁定期/减持路径/税务筹划 | 纸面价值 ≥1 亿后按变现折扣复算落袋额；启动退出流程 |

### 5. 铁律（与 roadmap.md 保持一致，每次执行本方案都必须对照）

1. 保留 2 年家庭现金流，不 all-in。
2. 融资节奏永远跑在现金流断裂前 12 个月。
3. 纸面 1 亿 ≠ 落袋 1 亿：稀释、对赌、锁定期全部计入复算。
4. 每个阶段预设 kill criteria，触发即换，不恋战。
5. 方案内数字均为框架参考，**具体指标、融资节奏、估值时机由子技能 B（one-hundred-million-path-review）每 90 天按 checkpoints.md 动态校准**；拿不准的不写死、不承诺、不编造。

### 6. 与子技能的分工

- 方案执行动作落在两个子技能：内容获客与产品转化 → 子技能 A（短视频制作，见下）；路径复盘与决策审查 → 子技能 B（one-hundred-million-path-review）。
- 每期短视频数据（metrics.csv）同时反哺选题与「线索 → 转化」记录，作为阶段 0/1 的进度证据；作者提「复盘进度 / 换方向 / 融资 / 退出」时切换到子技能 B 执行。

## 三、子技能 A：短视频制作（内容获客）

### Overview

本 Skill 用于制作"Map 的架构师训练营"系列短视频：从作者提供的资料或网络知识出发，产出可直接发布到主流短视频平台（抖音、微信公众号、小红书、YouTube、快手等）的成片，包含口播稿、视频画面、配音、左上角水印（作者：@Map）、分级字幕、前 3 秒封面。作者**默认发布渠道为微信公众号与抖音**（见固定规范第 18 条与 Step 8 一键发布），其余平台按需派生物料。

### 支持的训练营类型

固定 **9 大类**：架构师训练营、大数据训练营、AI 训练营、产品经理训练营、前端训练营、测试训练营、管理训练营、软技能训练营、以及**读书训练营**（一本书拆上/中/下 三部、按序推进的独立内容线，模型见下方「读书训练营专项」）。类型影响封面设计与开场话术，不影响流程。**任何主题一律归入这 9 类中最接近的一类，不另设「其他/兜底」分类**（归类决策见固定规范第 16 条第 ② 项）。

### 训练营主题色（封面与幻灯片必须遵循，不可混用）

每个训练营一个主色，取色自红橙黄绿青蓝紫色系，封面统一用**同色系渐变**（深底 → 主色），标题与装饰元素用高亮色。

| 训练营 | 主色 | 封面渐变（底色 → 主色） | 高亮/装饰色 |
|--------|------|--------------------------|--------------|
| 架构师训练营 | 纯绿 | `#00572E` → `#00C853` | `#4ADE80` |
| 大数据训练营 | 纯蓝 | `#0A2A6B` → `#0A5CFF` | `#4D8DFF` |
| AI 训练营 | 纯红 | `#7A0010` → `#E60012` | `#FF4D4F` |
| 产品经理训练营 | 紫 | `#3C0A7A` → `#8B2BFF` | `#B47CFF` |
| 前端训练营 | 橙 | `#B33C00` → `#FF7A00` | `#FFA94D` |
| 测试训练营 | 青 | `#00555C` → `#00C2C7` | `#4FE3E8` |
| 管理训练营 | 黄 | `#B37A00` → `#FFC400` | `#FFE066` |
| 软技能训练营 | 玫红 | `#8A0B45` → `#FF3D7F` | `#FF7FB0` |
| 读书训练营 | 书墨棕 | `#4E342E` → `#A1887F` | `#D7CCC8` |

执行要求：封面底色取渐变深端、主色取亮端，大标题与文字用纯白或高亮色并保证对比度（黄/橙系务必深底白字）；HTML 幻灯片的强调色、卡片数字、进度条统一沿用该训练营高亮色；同一支视频内不得混入其他训练营的主色。

**封面背景模板（资产来源·已确认）**：封面背景模板统一存放在项目资产 `项目 / 资产 / We-Media`（封面模板目录）下，按训练营取对应背景色——**架构师训练营=绿色、大数据训练营=蓝色、产品经理训练营=紫色、AI 训练营=红色**（与上方主题色表一致；其余训练营沿用各自主题色）。生成封面时优先套用该目录内对应训练营的官方模板背景，避免每次手工配色偏差；模板缺失时按主题色表渐变兜底。

### 读书训练营专项（一本书拆上/中/下，按序推进）

读书训练营是独立于 8 类技术训练营的内容线，产出节奏遵循「一书三分」模型，**必须严格遵守，不得自由选部、不得每周荐书**；**三部默认一次性连续生成（见「批量生成」规则），不按周等更**：

- **一书三分**：选定「一本书」作为当期读书训练营书目，将全书拆为「上 / 中 / 下」三部。
- **单部时长（强制·≥30 分钟）**：读书训练营每部（上/中/下）成片时长**必须 ≥ 30 分钟（1800 秒）**——这是「替观众把一本书读透、讲清精华」的最低门槛，不是短视频档位。每部独立走完整 9 步流程与四方同频门禁；因单部 > 30 分钟，按固定规范第 17 条**派生切条**（把成片按需切成多条短素材做分发，见 Step 8 第 1 条）。**页数与节奏**：每部 **30–50 页**（首行封面 + 内容页），单页停留 **40–70 秒**，按书中章节逐页深挖、配原创示意图与权威数据；禁止用「少页长停留」或空画面凑时长。内容密度、正确性、去侵权等硬约束不变（正确性优先于完整性）。单部时长以 `build/segments_durations.json` 的 `total` 为准，< 1800s 视为不合格、必须重做。
- **顺序推进（内容）+ 批量生成（产出）**：内容叙事必须按 上 → 中 → 下 顺序推进（不得跳部、不得倒序）；但**三部默认一次性连续生成**——同一运行内依次产出上、中、下三部成片，共用素材核验与渲染管线，不必等每周再开工。发布/铺量可按周错峰，但**生成不卡周**。接到读书训练营任务即按此批量生成，除非作者明确指定只产某一部。
- **批量生成清单**：一次性生成三部时，建三个以各分部标题命名的文件夹（如《数据密集型应用系统设计（上）：…》《（中）：…》《（下）：…》），每部独立走 9 步流程与四方同频门禁；进度文件 `reading_camp_progress.json` 用 `partsDone`（已生成分部列表，如 [上,中,下]）记录进度，三部全成后写 `finishedAt`、方可换书。
- **换书规则**：当前书的三部全部产出完毕（下部收官）后，才推荐/选定「下一本书」，并从新书「上部」重新开始；书未读完（处于上/中部）时不换书、不重新荐书。
- **选题拆解**：每一部的选题方向基于该部对应的书中章节内容拆 2–3 条（开篇钩子 / 反直觉观点 / 实践落地），附书中对应章节依据，禁止脱离书本自由发挥。
- **与短视频错位**：读书训练营选题（原理拆解/动手）与同日 8 类训练营短视频选题（热点议题）保持题材错位，避免撞车。
- **进度状态**：用进度文件（如工作区 `reading_camp_progress.json`，字段：`currentBook` / `partsDone`(已生成分部列表, 如 [上,中,下]) / `finishedAt`）记录「当期书 + 已生成分部」；批量生成时三部依次写入 `partsDone`；三部全成后写 `finishedAt`，方可换下一本书（归零为「新书·上部」）。
- **核心内容入画（精华提炼 + 书中图示，强制）**：读书训练营的本质是「替观众把一本书读透、讲清精华」，不是泛泛念稿——每部必须把对应章节的**核心结论、精华要点、关键图示**提炼进画面，**禁止只有标题页 + 口播空画面**。（1）原书关键架构图/对比图/流程图的**概念必须进画面**，但以「忠于原书概念、自绘原创示意图」方式呈现，**不照搬原书图稿的精确像素/排版**，避免版权侵权（遵守固定规范第 14 条图示去侵权与「图示来源可查」要求）；必要时应画面/口播标注「示意图，基于《书名》第 N 章」。（2）书中核心数据、性能倍数、结论须用原书权威内容并与公网权威来源逐项核对，正确性优先于完整性，禁止编造。（3）图示可查来源 = 原书对应章节（口播/画面标注章节出处），与原书冲突以官方勘误/最新版为准。
- 读书训练营视频同样走本技能「三」的 9 步流程（Step 0～Step 8），封面/水印/字幕/画幅等全部硬约束不变。

### 固定规范（每次必须遵守，不可省略）

1. 口播稿开头固定：`大家好，我是Map。`
2. **结尾禁止固定结束语模板（强制）**：禁止出现"以上就是今天所有内容，感谢大家观看，我们下期再见"等固定收尾句（任何变体都不允许）；结尾必须自然收尾 + 引导关注/评论的 CTA（CTA 话术见 `references/hooks-and-cta.md`）。**该禁止同样适用于幻灯片画面文字**：PPT HTML（封面、结尾页、页脚、角标）中不得出现此类固定收尾句，导出帧前必须扫描 PPT.html 全文并删除对应元素（见 Quality Gates「PPT 画面禁固定句扫描」）。
3. 全文禁止出现"附件中内容提到""资料里提到""根据文档"等字眼——内容就是作者自己的创作，直接以第一人称讲述。
4. 标题不超过 20 个字。
5. 视频左上角必须有水印：`作者：@Map`，全程可见。**水印不得遮挡任何画面内容**：字幕、幻灯片文字/标题、封面标题、架构图等图示主体都不能被水印盖住；排版内容时必须为左上角水印预留安全区（排版在前、水印落位在后，内容与水印错开），任何一帧不得出现水印压字的情况。
5.1 **档位由技能自主判断（强制·不默认 S、不向作者确认）**：档位定义 S（60–90秒）/ M（3–5分钟）/ L（15–30分钟）与结构模板见 `references/content-formats.md`；**读书训练营 上/中/下 为体系化长篇，单部 ≥30 分钟，不受 L 档 15–30 上限约束**（规则见读书训练营专项「单部时长」）。**档位不再默认 S**——由技能基于选题经验自主裁定 S 或 M（判断依据见下方「档位自主决策原则」）；L 档仅用于长课 / 训练营导流等按需场景，不默认。账号整体配比目标仍参考 70% S / 20% M / 10% L，但这是**账户级统计目标**，不是单条默认——技能自主选档时优先保证内容讲透与获客漏斗价值，不被配比数字绑架。

**档位自主决策原则**：①选 **S 档**——单一反常识结论、热点事件速评、纯认知冲击、争议话题、单点技巧 / 避坑，信息密度低、无需展开原理，60–90 秒可讲清；②选 **M 档**——需讲原理 / 架构对比 / 数据验证 / 案例拆解 / 方法论，或高客单服务导向（讲透以建立信任），3–5 分钟方能讲透；③**L 档**按需（长课 / 训练营导流），不默认。判断时结合 `metrics.csv` 历史高效档位与获客漏斗价值：带意向线索、需建立专业信任的主题偏 M；纯破圈涨粉、单点冲击偏 S。
5.2 **前 3 秒必须是口播 + 画面的双钩子**，结尾自然收尾时必须加引导关注/评论的 CTA（禁止固定结束语模板），模板见 `references/hooks-and-cta.md`。
6. 视频开头前 3 秒必须是可直接使用的封面（含标题、主视觉），静态或轻微动效均可。**封面不得出现训练营名称/期号等标识**（见固定规范第 14 条）。
7. **内容必须忠于权威来源，禁止编造**：涉及的技术、数据、性能倍数、结论必须来自可核验的权威渠道（官网/官方文档/官方博客/官方论文/权威技术媒体），拿不准的略过或留白，正确性优先于完整性。**素材双重来源（强制）**：项目资产不是唯一资料来源——即使资产中已有对应内容，也必须再到公网权威网站获取同一主题的官方内容进行核对与补充（以官方最新版本/数据为准），不得仅凭资产资料直接制作（执行细节见 Step 1 第 1 条）。
8. 语言必须自然口语化，看不出 AI 味（详见 references/script-writing-guide.md）。
9. 成片必须经作者审核通过后才可发布到短视频平台；未获批准前只交付文件，不执行任何发布动作。
10. 配音音色固定（强制）：所有视频统一使用固定音色 `zh-CN-YunxiNeural`（Azure 神经网络语音），不随训练营/项目变化，不依赖任何外部声音模版文件；每次生成视频都使用这一固定音色，禁止自行随意更换音色。
11. **字幕、声音、内容、画面四方必须逐字一致且同频（强制）**：口播稿 → 配音 → 字幕 → 当前画面，四方文本对得上且时间片重合。具体要求：
   - **字幕文本 = 配音真实朗读（逐字）**：TTS 用 `scripts/gen_sync_subs.py` 基于 edge-tts 词级 boundary / `--write-subtitles` VTT 真实时间轴产出的逐字字幕；真人配音用 faster-whisper 词级时间戳对齐。配音不得擅自增删改口播稿内容；改稿后必须重配音并同步字幕。
   - **时间轴禁止估算（强制）**：字幕起止时刻与画面切换时刻**必须来自配音真实音频时间**，禁止「总时长÷总字数」按字数比例估算、禁止整篇一次性 TTS 后切分。正确做法：口播稿按幻灯片切 N 段（封面 1 段 + 每内容页 1 段），逐段 TTS，每段真实时长写入 `build/segments_durations.json`，字幕与画面均以此驱动（见 `scripts/gen_sync_subs.py`）。
   - **画面与声音同帧（强制）**：第 k 页幻灯片只在第 k 段配音播放期间显示，该段字幕也只在此时间窗内出现——「页面在播什么、声音讲什么、字幕显什么」三方共用同一时间片；交付前用 `scripts/check_sync.py` 自动校验（exit 0 才允许交付）。
12. **统一输出目录（强制）**：本技能的所有产出物——口播稿（txt）、HTML 幻灯片（html）、视频成片（mp4）等最终交付物——必须统一放在**同一个文件夹**里，**文件夹名称 = 视频标题**（即打磨后的发布标题，≤20 字）。路径规则：`工作区/<视频标题>/`。示例：标题《Doris读写分离：成本直降90%的秘密》→ 文件夹 `Doris读写分离：成本直降90%的秘密/`，内含 `Doris读写分离：成本直降90%的秘密_口播稿.txt`、`Doris读写分离：成本直降90%的秘密_PPT.html`、`Doris读写分离：成本直降90%的秘密_成片.mp4`。规则细节：
   - 文件夹名 = 当前使用的标题；标题含文件系统非法字符（`/ \ : * ? " < > |`）时替换为全角字符或下划线，并保持三份产出命名一致。
   - 配音、字幕、帧、分段等**中间产物**仍放工作区 `build/`，不进标题文件夹；标题文件夹只放最终交付物。
   - Step 7 标题终审若改动标题，必须同步重命名整个文件夹及内部文件，始终保持「文件夹名 = 视频标题」。
   - 同一主题重制视频时，覆盖更新标题文件夹内文件，不另起文件夹。
13. **视频一律默认 16:9 横屏（强制）**：所有成片默认 1920×1080（16:9）横屏，**不因目标平台自动改变画幅**——即使目标平台是抖音/快手/小红书等竖屏友好平台，默认仍输出 16:9 横屏。仅当作者**明确指定**其他格式（如明确要求 9:16 竖屏）时才按作者要求产出；目标平台信息只影响发布文案与派生物料，不影响默认画幅（执行细节见 Step 6 第 3 条、Step 8 第 1 条）。
14. **画面禁止出现训练营标识与期号（强制）**：成片画面（封面、正文帧、片尾）**不得出现任何训练营名称、期号、系列名等标识信息**——如"架构师训练营""第 1 期""架构师训练营 · 第 1 期"等字样/徽章一律禁止出现在视频画面里（口播稿中也不得念出）。训练营主题色仍正常用于配色与视觉风格（见"训练营主题色"表），但训练营名称/期号**只存在于**后台管理（metrics.csv）、封面文件名、发布文案等非画面位置；页脚、角标、进度条等画面元素不得出现训练营名或期号字样（页脚可保留标题或留空）。
15. **字幕带及字幕以下严禁放置任何画面内容（强制）**：画面底部「字幕带」（约 y 85%~98% 高度，字幕实际落位 y≈88%~95%）是字幕专用区，**字幕以下 / 字幕带之内不得放任何相关内容**——标题、要点文字、图示主体、卡片、封面标题、页脚说明等都不得落入该区域，否则字幕一出现就会把内容盖住。这是排版侧的硬约束，不是合成后再修。执行要求：①写 HTML 幻灯片时，内容主体（标题/要点/图示/卡片）必须收在画面顶部约 85% 以内，底部约 12%~15% 留白专供字幕；内容偏多时上移或缩减，绝不与字幕争地；②字幕渲染固定底部单行（见 Step 5 第 4 条）；③合成后抽帧校验——字幕带区域内无任何画面内容被压住，发现内容侵入字幕带立即调整（内容上移/缩小）后重新合成，不得带遮挡交付。
16. **自主决策·能自己判断就自己判断（强制）**：本技能以「自主」为运行基调——凡能自行判断的节点（档位、训练营归类、素材来源、目标平台、目标时长、初始标题锚点、选题方向等）**直接决策并推进，不向作者逐一确认、不阻塞等待**。各节点默认决策：①档位 → 按固定规范第 5.1「档位自主决策原则」自主选 S 或 M（**不默认 S**）；②训练营类型 → 由主题归入 9 大类中最接近的一类（见「训练营主题色」表），**不设「其他计算机方向」兜底类**；③素材来源 → 资产优先、缺失则公网权威双源核验；④目标平台 → 抖音（横屏母版不变，见固定规范第 13 条）；⑤目标时长 → 跟随所选档位（S 60–90s / M 3–5min / L 15–30min）；⑥初始标题 → 作者已给主题则以主题作锚点、未给则取自选选题中预估传播力最高者；⑦选题 → 自选预估传播力最高者。自主决策须在对应产物 / 日志中记录所用判断与默认值，便于作者事后回看或纠偏。自主推进仍须遵守本技能全部硬约束（字幕/水印/画幅/去侵权/内容正确/固定音色等），仅跳过「等待作者确认」这一步，不跳过任何质量门禁。**唯一必须等作者决策的节点（不适用自主规则，仍须等作者）**：Step 7 发布关卡——成片**发布到抖音 / 微信公众号之前**，必须让作者观看成片并明确批准；技能不得自行发布，无论等待多久，发布动作都须等作者明确决策（见固定规范第 9、18 条）。

17. **切片/切条仅超长视频生成（强制）**：单个视频总时长 **≤ 30 分钟（1800 秒）时，不生成切片/切条**（不把成片按段裁成多条短素材）；只有单个视频总时长 **> 30 分钟**才派生切条。判定以 `build/segments_durations.json` 的 `total` 为准。竖屏版等画幅规格派生不受此限（见固定规范第 13 条）。

18. **发布渠道与一键发布（强制·默认微信公众号 + 抖音）**：作者当前默认发布渠道为 **微信公众号** 与 **抖音**（其余平台暂不作为默认发布目标，百度百家号等可作为补充发布目标）。成片经 Step 7 作者明确审核通过后，支持**一键发布**到这两个渠道；一键发布依赖对应平台的开放接口/已授权连接器，未接入时改为交付「发布就绪包」由作者手动发布（见 Step 8 第 1.1 条）。发布动作始终受 Step 7 硬门禁约束——未经作者明确批准绝不触发。

19. **发布后归档百度网盘「自媒体目录」（强制·整个文件夹）**：成片经 Step 7 作者批准后（凡发布到任意自媒体平台——微信公众号、抖音、百度百家号等——均须触发），**必须把整个视频标题文件夹归档到百度网盘**，不是只传一个 mp4。目标路径固定为 `/自媒体/<训练营名>/<视频标题>/`——「自媒体目录」按 9 大类分类建档：`架构师训练营` / `大数据训练营` / `AI 训练营` / `产品经理训练营` / `前端训练营` / `测试训练营` / `管理训练营` / `软技能训练营` / `读书训练营`，视频归入其所属类别子目录（例：产品经理训练营视频 → `/自媒体/产品经理训练营/<视频标题>/`）。归档内容 = 主交付（成片 / PPT / 口播稿 / 完整工程 ZIP）+ 发布物料（封面 + 各平台文案）+ 工程源（分段稿 / 幻灯片源码 / 字幕 / 配音），共 13 项，命名与层级规范见 `references/netdisk-archive.md`。**百度网盘连接器已接入**，按该文档的 SOP 自动完成归档（完整工程包走「临时静态托管 → URL 转存 → 立即下线」），归档后向作者回报落盘路径。**未经 Step 7 批准的成片不得上传**（连公网托管跳板也不得使用）。此动作用于「发布完成 → 网盘归档」闭环，独立于一键发布。

20. **定时轮转生产模式（强制）**：本技能被定时任务（每 2 小时一次）调用时，**每次只生产「一大类」训练营的内容**，按 9 类固定顺序轮转——不一次跑多类、不重复、不跳类。轮转顺序、状态文件（`one-hundred-million-rotation.json`）、选类逻辑、产出后写回见下方「定时轮转生产模式」小节。该模式下的选题依据、自主决策基调、发布门禁、硬约束**全部沿用本技能既有规范**（第 9/16/18/19 条与 Step 0～Step 8），不另立规则。

21. **训练营体系化选题（强制·开放体系·章节无穷）**：每个训练营都是**一套体系化能力**——读者跟着看完这一整个训练营，应能成为该领域的专家（架构师训练营 → 架构师；大数据训练营 → 大数据专家；产品经理训练营 → 产品经理；AI / 前端 / 测试 / 管理 / 软技能同理）。**章节是无穷的，不存在固定目录**：`references/syllabus.md` 给每类训练营列的「能力维度 + 示例主题」只是**思路提示，不是边界、不是待办清单**。每条短视频都是该体系中的一个节点，须**覆盖专家能力地图的不同维度、与前后内容承接、不重复已讲、不游离成孤立知识点**；开工顺序固定为「**先定体系位置（往哪个能力维度延展）→ 再出题目**」（Step 0 第 1 条）。机制细则见「训练营体系化选题（开放体系）」小节。

22. **每个训练营都要在项目资产维护一份已生成短视频的标题目录（强制·tdrive）**：与固定规范第 19 条「发布后归档百度网盘」相对，本条要求每个训练营（9 大类）都有一份**长期留存、跨会话可查**的标题目录索引，存放在 **tdrive 项目资产根级 `短视频目录/`**（与 `We-Media`、`InfoQ` 平级），下设 `00_总览.md` + `01_~09_<大类>.md` 共 10 个 md。维护机制见 Step 8 第 2.2 条「分类目录维护」与 `references/catalog-sync.md`：①本地事实来源由 `scripts/update_catalog.py` 自动写入工作区 `目录/`（每次 Step 8 自动调）；②项目资产副本按 SOP 同步到 tdrive `短视频目录/`（手动场景经作者确认执行；cron/定时场景默认跳过并标注「待手动触发」，下次交互式会话补同步）。**首次启用前须在项目资产建好 `短视频目录/` 并把 dir_id 配置到 `scripts/sync_catalog_tdrive.py` 顶部 `TDRIVE_DIR_ID`**（建目录属 Project Drive mutating，须经作者确认）。这是「方便后面查阅」——日后按训练营大类（如「架构师训练营」）查看已生成过哪些视频、各期讲什么能力维度，配合 `one-hundred-million-syllabus.json` 的 `covered` 掌握体系覆盖进度。

23. **模型使用与限流降级（强制·免费优先 + 自动切换）**：内容生成**优先使用免费模型**；某个模型「使用量超出频率限制 / credit 额度用完」时，**不询问、不停止、不阻塞**，自动切到下一个未耗尽的免费模型继续跑；**全部免费模型耗尽才回退 `Auto`**（计费）并在日志标注。完整 SOP 见 `references/model-fallback.md`，执行靠 `scripts/pick_free_model.py`：

    - **免费清单禁止硬编码**：由服务端下发，每次用脚本实时判定。实时目录 = 本机缓存 `~/.workbuddy/cache/acc-product-config-v3.json` 的 `models[]`，判定字段 `credits`：以 `x0.00` 开头 = 免费；`x0.05`/`x0.21` 等 = 计费；`null`/缺失 = **不计入候选**（无法判定是否免费）。2026-09-03 实测免费模型为 `hy4-preview`（Hy4 preview，100 万上下文 / 64k 输出 / 多模态 / 工具调用 / 推理）与 `hy3`（Hy3，192k / 64k），**该快照仅作参考，一律以实时目录为准**。
    - **限流信号**（命中任一即走降级，不要当脚本 bug 排查）：`credit 额度已用完`、`使用量超出频率限制`、`rate limit`、HTTP `429` / `Too Many Requests`、`quota exceeded`。脚本报错 / 文件缺失 / 网络超时重试成功**不算**限流——修问题本身，不切模型。
    - **标准动作**：①开工（Step -1）跑 `pick_free_model.py list` + `pick` 确认本轮模型；②命中限流 → `exhausted <模型id> --reason "<摘要>" --cooldown 7200` 标记冷却（默认 2h，与定时节奏对齐；响应带 reset 时间则以其覆盖）→ `pick` 取下一个可用免费模型；③**断点续跑**——已完成的确定性步骤（脚本 / TTS / ffmpeg / `check_sync` / 像素扫描）不回滚不重做，从被中断的 LLM 依赖步骤继续；④**门禁一条不减**——换模型不豁免 `check_sync.py` exit 0、禁句禁标识扫描、字幕带与水印区扫描、素材双源核验；⑤记录——run 日志与 `metrics.csv` 备注列写「模型：<id>｜切换：<from> → <to>｜原因：限流」。
    - **切换执行方式**：CLI 用 `/model <id>` 或 `--model <id>`；桌面端在模型下拉选（智能体没有直接改下拉的工具，**无人值守场景靠状态文件跨轮生效**——本轮继续跑确定性步骤，下一轮 `pick` 自动跳过冷却中的模型；作者在场时按 `list` 结果切一次即可）。
    - **状态文件**：工作区根 `one-hundred-million-model-fallback.json`（`exhausted` / `history` / `current`），与 `one-hundred-million-rotation.json` 同属事实来源，手工改动以文件为准。冷却到期自动释放，误标记用 `reset --id <模型id>` 释放。
    - 本条**唯一例外**仍是 Step 7 发布关卡：模型切换属生产内部动作可自动，但成片进入发布关卡后不得换模型重生成（会破坏已审核成品），发布一律等作者批准。

### 定时轮转生产模式（自动化任务驱动 · 每 2 小时一大类）

定时任务（自动化）对本技能的调用方式极简：仅「加载技能 one-hundred-million，每 2 小时执行一次」。**本技能的完整生产逻辑内建于此，定时任务不携带任何流程细节**——技能每次被定时调用时，按以下机制自行决定本次做哪一大类、做成什么样。**自动化每次执行前必须重新完整加载本 SKILL.md 并严格按其当前 Step 0～Step 8 全流程执行；若自动化自身历史记忆中的流程要点与本 SKILL.md 不一致，一律以本 SKILL.md 为准，禁止沿用旧管线**（旧 PIL 管线 `render_frames.py`/`gen_ppt.py` 出 base64 图片式 PPT 已废弃；Step 2 已改强制套 `templates/slide-template.html`，Step 6 已改用 `compose_motion.py`/`render_animated.js`）。

**1. 轮转顺序（9 大类固定，下标 0–8）**

| 下标 | 类别 | 下标 | 类别 |
|------|------|------|------|
| 0 | 架构师训练营 | 5 | 测试训练营 |
| 1 | 大数据训练营 | 6 | 管理训练营 |
| 2 | AI 训练营 | 7 | 软技能训练营 |
| 3 | 产品经理训练营 | 8 | 读书训练营 |
| 4 | 前端训练营 | | |

**2. 轮转状态文件**

- 路径：当前工作区根目录 `one-hundred-million-rotation.json`。
- 内容：`{"lastIndex": <上次生成的类别下标 0–8>, "lastCategory": "<上次类别名>", "lastTitle": "<上次视频标题>", "lastRunAt": "<ISO 时间>"}`。
- **文件不存在（首次运行）→ 本次从第 0 类（架构师训练营）开始**。

**3. 每次执行的选类逻辑**

读取状态文件 → 本次类别 = `(lastIndex + 1) % 9`（首次取 0）→ **仅生成该 1 大类的内容** → 产出完成后把 `lastIndex` / `lastCategory` / `lastTitle` / `lastRunAt` 写回该文件。类别推进以「本次产出完成（成片交付或进入 Step 7 发布关卡）」为准；Step 7 等待作者审批不阻塞状态写回。

**4. 读书训练营的例外（沿用其自身产出模型）**

轮转到下标 8（读书训练营）时，按本技能「读书训练营专项」的**「一书三分 · 批量生成」**规则执行——同一运行内连续产出上 / 中 / 下三部，仍属「只跑读书这一个大类」，不算多类。

**5. 本模式下的自主决策与门禁（沿用既有规范，不另立）**

- **自主决策（固定规范第 16 条）**：档位、训练营归类、素材来源、目标平台、目标时长、初始标题锚点、选题——全部技能自主判断推进，**不向作者逐项确认、不阻塞等待**。
- **选题依据**：①**体系定位优先（强制）**——先查该大类在 `references/syllabus.md` 的能力维度（示例提示，非固定章节）与 `one-hundred-million-syllabus.json` 的 `covered` 进度，确定本期往哪个尚未充分覆盖的能力维度延展，再围绕该维度（结合当期热点 / 案例）出题（见「训练营体系化选题（开放体系）」小节）；②该大类的「大类型」定位与专家目标；③**1 亿目标获客漏斗**（选题须能导向高客单服务 / 股权机会，不只涨粉）；④**当前热门技术 / 话题**（联网检索抖音 / 快手 / 小红书 / B站 / 公众号 / YouTube 近 30 天热点与新版本，用作讲该维度的**切口**）；⑤**历史数据反哺**（工作区 `metrics.csv` 的爆款钩子 / 高效档位 / Top 选题）。选题须落在该大类的**开放体系某维度**上、与已讲内容承接不重复、有真实受众、有前 3 秒传播钩子、能导向高客单服务、事实可核实（**禁止编造**）。
- **唯一需作者决策的节点 = Step 7 发布关卡**：成片发布到抖音 / 微信公众号之前，必须让作者观看成片并明确批准；未批准绝不发布、绝不触发任何发布动作（当前一键发布未接入，改为交付「发布就绪包」到视频标题文件夹 `发布/` 子目录）。
- **硬约束不变**：固定音色 `zh-CN-YunxiNeural`、默认 16:9 横屏、左上角水印「作者：@Map」、字幕 / 声音 / 内容 / 画面四方同频（`check_sync.py` exit 0 才交付）、画面无训练营标识与期号、批准后归档整个视频标题文件夹到百度网盘「自媒体目录」（第 19 条）。

**6. 产出后写回（每次必须）**

1. 追加一行到 `metrics.csv`（字段见 Step 8 第 2 条）。
2. **更新体系进度**：把本期讲到的能力维度 / 主题节点关键词与视频标题写回 `one-hundred-million-syllabus.json` 的 `covered`（开放列表，去重、非固定编号；跳过的主题不记录；不重复已覆盖节点）。
3. **分类目录维护**：调用 `update_catalog.py` 把本期视频追加进所属大类的 `目录/<大类>.md`（含总览刷新），见 Step 8 第 2.2 条。
4. 读书训练营：更新 `reading_camp_progress.json`（`currentBook` / `partsDone` / `finishedAt`）。
5. 写回 `one-hundred-million-rotation.json`（本次类别下标 + 类别名 + 标题 + 时间）。

**7. 纪律**

不臆造发布动作；不向作者逐项确认选题 / 档位；每次只做选中那一个大类；状态文件是轮转的唯一事实来源，手工改动以文件为准。

**8. 限流自动降级（无人值守 · 固定规范第 23 条在定时模式下的落地）**

定时任务最容易撞上限流（2 小时一轮、长时间连续生成）。本模式下按以下顺序执行，**全程不向作者确认**：

1. **开工 Step -1**：`python3 scripts/pick_free_model.py list --workspace <工作区>` 看免费模型与冷却状态；`pick` 取本轮应使用的模型（`0` = 可用免费 id，`2` = 免费全耗尽 → `AUTO`，`3` = 读不到配置 → 记日志并按 Auto 继续、标注待人工核对清单）。
2. **运行中命中限流信号**：立即 `exhausted <被限流模型id> --reason "<摘要>" --cooldown 7200`，再 `pick` 取下一个，**继续本轮后续步骤**；不得原地重试同一模型、不得中断本轮。
3. **不可自动改下拉怎么办**：智能体无法在会话内改模型下拉。无人值守时靠状态文件跨轮生效——本轮把已能做的确定性步骤（脚本生成 / TTS / ffmpeg / 校验 / 目录写回）做完，被 LLM 阻断的步骤记入日志；下一轮开工 `pick` 自动跳过冷却模型，落到下一个免费模型。
4. **全耗尽回退**：`pick` 返回 `AUTO` 时回退 Auto（计费）继续生产，日志与 `metrics.csv` 备注列标注「免费额度已耗尽，本期走计费模型」。
5. **写回**：轮转状态写回时一并记录本轮模型与切换记录（第 6 部分第 1、5 条）。

### 训练营体系化选题（开放体系 · 章节无穷 · 看完成为专家）

**核心命题**：每个训练营不是知识点的随机集合，而是一套**体系化能力**——读者跟着看完这一整个训练营，应该能成为该领域的专家（架构师训练营 → 成为架构师；大数据训练营 → 成为大数据专家；产品经理训练营 → 成为产品经理；前端 / 测试 / 管理 / 软技能 / AI 同理）。**章节是无穷的**：没有一份必须讲完的固定目录，任何新角度、新技术、新案例都能长成新的一条。体系化的抓手是——每条视频都**覆盖专家能力地图的不同维度、与前后内容承接、不重复已讲、不游离成孤立知识点**。

**1. 能力地图（示例，非边界）的位置**

各训练营的「专家目标 + 能力维度 + 示例主题」见 **`references/syllabus.md`**（明确标注：示例，非穷举、非待办清单）。读书训练营不在此列——**书本身就是目录**，按「读书训练营专项」的一书三分推进即可（天然体系化）。

**2. 选题机制：先定体系位置，再出题目（强制）**

开工时的顺序是 **先定位、后出题**，不可颠倒：

1. 读 `references/syllabus.md` 中该训练营的能力维度（作为「还能往哪些方向延展」的提示，不锁定章节）；
2. 读进度文件 `one-hundred-million-syllabus.json`（工作区根目录，不存在则该训练营 `covered` 为空、从最基础维度起）；
3. **确定本期要往哪个能力维度延展**（优先 `covered` 尚未充分覆盖的维度；当期热点 / 新数据能映射到某维度时切入该维度，仍属体系内；不重复已讲主题）；
4. 围绕**这一维度（结合当期热点 / 案例）**产出候选选题、定标题、找素材——**禁止**长期扎在同一个窄点、或做与体系无关的孤立零散点。

**3. 三条纪律**

- **体系连贯推进，不扎堆、不空白**：覆盖专家能力地图的不同维度，不长期卡在某一个窄话题、不整块能力空白（如只讲前端不讲测试）；允许因热点 / 时效往某维度提前延展，但须记录，不得长期偏离体系主线。
- **能力地图是骨架，热点是切口**：能力维度决定「往哪个方向讲」，热点 / 案例 / 数据决定「怎么讲得有人看」——用当期的真实案例与热点去讲体系中该维度的内容，**不为追热点把体系打散成零散知识点**。
- **体系感体现在内容，不体现在画面**：内容承接（上一条讲什么、这一条接着讲什么）与后台进度记录负责体系感；**画面与口播仍严格遵守固定规范第 14 条**——不得出现「第 X 章」「第 X 节」「第 X 期」等编号标识与训练营名称，封面 / 页脚 / 角标同样禁止。

**4. 与其余机制的衔接**

- **定时轮转模式**：每 2 小时轮转到某一大类时，先读该类的进度（`covered`），用「下一个尚未充分覆盖的能力维度」决定本期选题（见「定时轮转生产模式」第 5 部分）。
- **自主决策**：体系定位由技能自主完成，不向作者逐一确认（固定规范第 16 条）；仅在作者给出初始标题时，把该标题**归入该训练营的能力地图某维度**再开工。
- **发布价值判断**：Step 0 的 7 维判断不变，但第 7 维「体系归属」改为——**该选题能否纳入该训练营的开放体系**（与已讲内容形成承接 / 递进 / 互补，不重复、不游离）？游离体系之外、是孤立零散点的主题 = 打散体系，判为不合格，改回体系内选题。
- **产出后写回**：成片进入 Step 7 后，把本期覆盖的能力维度 / 主题节点关键词与标题写回 `one-hundred-million-syllabus.json` 的 `covered`。

**5. 体系永远在生长**

章节无穷，没有「目录讲完」这一说。实践中发现某训练营可延展的新主题、新技术、新案例，**随时可长出来**，无需预列入 `references/syllabus.md`；`syllabus.md` 的示例主题若已过时或被新技术取代，技能直接用更合适的当下主题替代，并只把实际讲过的节点记进进度文件 `covered`，不回写示例表。

### 配套技能与连接器

| 资源 | 状态 | 在本流程中的作用 | 使用位置 |
|------|------|------------------|----------|
| `humanizer-zh`（内置插件技能） | 已可用 | 去除口播稿中的 AI 生成痕迹，让语言更像人写 | Step 1 写完后 |
| `remotion-video-toolkit`（用户级） | 已安装 | React/Remotion 程序化视频：需要复杂字幕动画、转场、云端渲染时的可选替代路径 | Step 5/6（默认仍走 ffmpeg） |
| `multi-wordcheck` 多平台违禁词检测（用户级） | 已安装·**默认略过（非强制门禁）** | 三平台（公众号/小红书/抖音）违禁词官方检测，纯 API 依赖 `REDFOX_API_KEY`；**无 key 时本流程默认略过，不做强制门禁**，作者明确要求执行时才调用 | Step 7（可选·默认略过） |
| 腾讯文档 `tencent-docs` 连接器 | 待连接 | 口播稿在线协作，作者批注修改意见，审核留痕 | Step 1 / Step 7 |
| `cn-last30days` 社媒话题研究（用户级） | 已安装 v2.0.1 | 近 30 天小红书/抖音/公众号跨平台热点与舆情趋势，供 Step 0 选题 | Step 0 |
| 百度网盘 `baidu-netdisk` 连接器 | **已连接** | **发布后归档（强制）**：把**整个视频标题文件夹**（13 项物料）归档到 `/自媒体/<训练营名>/<视频标题>/`。**能力边界**：无本地二进制直传、无删除、无文件夹上传——二进制一律走「临时静态托管 → `file_upload_by_url` → 立即下线」，完整 SOP 与编码避坑见 `references/netdisk-archive.md` | Step 8（发布后归档） |
| 微云 `tencent-weiyun` 连接器 | 待连接 | 素材、配音、成片的云端备用存取 | Step 1 / Step 3 |
| 微信公众号发布连接器 | 待接入 | 成片一键发布到公众号（视频+标题+正文文案）；作者当前默认发布渠道之一 | Step 8（一键发布） |
| 抖音开放平台发布连接器 | 待接入 | 成片一键发布到抖音（视频+标题+话题标签）；作者当前默认发布渠道之一 | Step 8（一键发布） |
| 百度百家号发布连接器 | 待接入 | 成片一键发布到百度百家号（视频+标题+正文）；作者可选的自媒体平台之一，发布后同样触发固定规范第 19 条归档 | Step 8（一键发布） |
| 项目资产（tdrive）连接器 | **已连接** | **目录归档（强制 · 固定规范第 22 条）**：把本地 `目录/*.md`（10 个）幂等同步到 tdrive 项目资产根级 `短视频目录/`，让每个训练营的标题目录在项目资产里长期留存、跨会话可查。**触发边界**：交互式（手动）场景经作者确认后执行；cron/定时场景默认跳过并标注「待手动触发」。完整 SOP（含首次建目录、`search_file`/`file_download` 比对、`file_upload`(overwrite) + curl PUT + `file_upload_complete`、与 `update_catalog.py` 解耦）见 `references/catalog-sync.md`；首次启用前须在项目资产根目录建 `短视频目录/` 并把 dir_id 配置到 `scripts/sync_catalog_tdrive.py` 顶部 `TDRIVE_DIR_ID` | Step 8.2.2（项目资产副本） |

连接器若未连接，引导作者在「设置 - 连接器」中授权；未连接时退化为本地路径 + 项目资产，不影响主流程。

### 工作流程

按顺序执行以下 9 步（Step 0～Step 8）。**本技能以自主运行为基调（见固定规范第 16 条）：档位、训练营归类、素材来源、目标平台、目标时长、初始标题锚点、选题方向等均由技能基于经验直接判断并推进，不逐项向作者确认**；视频格式默认一律 16:9 横屏，仅当作者明确指定其他格式时才按作者要求产出（见固定规范第 13 条）。**唯一需作者决策的节点是 Step 7 发布关卡**——成片发布到抖音 / 微信公众号前，必须先让作者观看成片并明确批准（见固定规范第 16 条例外）。

#### Step 0：选题引擎（开工前的第一步，不可跳过）

1. **体系定位（强制·先定体系位置再出题）**：**先读 `references/syllabus.md`** 中本期训练营的能力维度（示例提示，非固定章节），再读进度文件 `one-hundred-million-syllabus.json` 中该训练营的 `covered`（已覆盖主题节点），**确定本期要往哪个尚未充分覆盖的能力维度延展**——优先 `covered` 未覆盖的维度；当期热点 / 新数据能映射到某维度时切入该维度（仍属体系内，须记录）；不重复已讲主题。读书训练营不查此文件，改读 `reading_camp_progress.json` 按书中章节推进。后续所有候选选题**只能围绕这一体系位置（结合当期热点）**展开，游离体系之外的孤立零散点一律不进入清单。详见「训练营体系化选题（开放体系）」小节。
2. **先读历史数据**：若已存在 `metrics.csv`，按 `references/multi-platform-repack.md` 的反哺规则提取爆款钩子类型、高效档位、Top 选题方向，作为本期选题依据。
3. **热点检索**：用 `cn-last30days` 技能（近 30 天小红书/抖音/公众号跨平台热点）或 WebSearch 检索主题相关热点、行业事件、新版本发布。
4. **竞品爆款拆解**：检索同赛道头部账号近 30 天的高赞内容，拆解其钩子类型、结构、时长、争议点。
5. **产出选题清单**：给 3–5 个候选选题，每个标注 —— 所属能力维度 / 体系位置（如「架构师·高并发」「前端·性能」）/ 一句话角度 / 建议档位（S/M/L）/ 预估传播力（高/中/低）/ 建议钩子类型 / 风险点（是否有争议或事实不确定）。
6. **技能自主选定选题**：基于选题清单与「初始标题发布价值判断」，技能**自主选定**预估传播力最高且获客漏斗价值最大的候选进入 Step 1（多候选并列时用热点时效 / 争议性破平）；选题决策记录在产物 / 日志中，便于作者事后回看或纠偏。不再向作者逐一确认选题（作者可随时插话纠偏）。

**初始标题规则**：作者会给出一个初始标题作为主题锚点（示例：`Doris读写分离`）。若作者未提供初始标题，先询问作者；发出询问后 3 分钟内未答复，适用固定规范第 16 条：以作者已给主题（或 Step 0 自动选定的选题）作标题锚点继续，并记录该自动决策，不阻塞。初始标题可直用或优化，最终发布标题在其基础上打磨（≤20 字，公式见 references/script-writing-guide.md），封面、开场钩子均围绕初始标题展开。

**初始标题发布价值判断（强制）**：作者给出初始标题后，**不得直接开工**，必须先判断该标题/主题是否有发布价值。判断维度（至少覆盖以下 7 项）：
1. **垂直相关性**：主题是否落在账号定位与训练营矩阵内（架构师/大数据/AI/产品经理/前端/测试/管理/软技能/读书训练营）？完全无关的主题即使有流量也不做——不为大号导流、不稀释账号心智。
2. **受众规模**：主题是否有真实受众与讨论/搜索量？用本技能绑定的资源核实（`cn-last30days` 近 30 天跨平台热点检索、WebSearch 行业动态、竞品账号爆款拆解），查不到讨论量的主题默认无价值。
3. **传播钩子**：是否有反常识结论、硬核痛点、明确数据对比、时效热点等可提炼成前 3 秒钩子的爆点？平铺直叙、无冲突无反差的角度判价值不足。
4. **获客漏斗价值**：主题是否能自然导向作者可交付的高客单服务（各训练营/企业内训/咨询/陪跑）？只涨粉不带意向线索的纯蹭热点主题降权。
5. **事实可核实性**：涉及的数据、性能倍数、技术结论是否有官网/官方文档等权威来源可核对？无来源、无法核实的夸张结论（如"XX 一统天下"）直接判无价值——禁止编造。
6. **数据反哺**：与 `metrics.csv` 历史爆款方向、高效档位、Top 选题是否一致。
7. **体系归属（强制）**：该主题能否纳入该训练营的**开放体系**（`references/syllabus.md` 的能力维度地图）？能纳入 → 归入某一能力维度（如「架构师·高并发」「前端·性能」），并检查该维度下该主题是否已在 `covered` 中（已讲过则换角度 / 新数据或顺延到未覆盖维度）；**游离体系之外、与已讲内容无承接 / 递进 / 互补关系的孤立零散点主题 = 打散体系，判为不合格**，改回体系内选题，不做。

判断结论分级处理：
- **有价值** → 按原流程打磨标题、进入 Step 1 开工。
- **价值不足或无价值** → **不得直接照做**，必须用本技能绑定的技能与连接器检索并替换主题：`cn-last30days`（近 30 天小红书/抖音/公众号跨平台热点）、WebSearch（行业事件/新版本发布/官方动态）、竞品爆款拆解（同赛道头部账号高赞内容），从检索结果中提炼该主题的衍生角度或同赛道高热度主题，按 Step 0 第 5 条格式产出 **3–5 个替代选题**（且这些替代选题同样须落在该训练营的开放体系内、归入某一能力维度）（含所属维度/一句话角度/建议档位/预估传播力/建议钩子/风险点）交作者重新拍板；作者选定替代主题后，初始标题锚点同步替换为选定的新主题。

价值判断结论（原标题评价 + 替代选题 + 作者最终选择）需记录，供 metrics.csv 复盘与后续选题反哺使用。

#### Step 1：获取素材并生成口播稿

1. **双重来源获取素材（强制）**：项目资产是素材起点，但**不能只依赖资产**——即使资产中已有对应内容（文档/PPT/PDF/视频），也必须再到公网权威渠道（官网/官方文档/官方博客/官方论文/权威技术媒体）获取同一主题的官方内容进行**核对与补充**，以公网权威来源校准资产资料的正确性与时效性（版本号、数据、性能结论一律以官方最新为准，资产与官方冲突时以官方为准并说明差异）。获取方式：资产用 `tdrive.search_file` / `tdrive.search_content` 检索关键词、`tdrive.file_download` 拿到下载链接后 curl 下载（docx 用 python-docx 或 zipfile 解析 `word/document.xml` 提取正文；pdf 用 pymupdf 提取文本）；公网用 WebSearch 定位权威渠道后下载原文。有价值的配图（架构图、对比图）一并提取裁切备用。
2. 若无素材：用 WebSearch 从**权威渠道**（官网/官方文档/官方博客/官方论文/权威技术媒体）检索主题的最新知识，**多源交叉验证**（至少两个独立权威来源一致才采信）后再用，优先官方一手资料而非二手转述；若内容涉及架构、部署、原理等需要图示的地方，**必须从官网/官方文档/官方博客等官方渠道检索并下载官方架构图备用**（架构图优先官方原图，保证权威与准确）。**下载后立即检查并清理图中可能涉及侵权的元素**（去侵权规则见 Step 2 第 5 条）。
2.1 **内容质量三要求（强制）**：成稿内容必须同时满足——①**无废话**：每一句都有信息量，可有可无的过渡句、重复解释、空话套话一律删除（逐句审查：删掉后不影响理解与信息完整性的句子即为废话，必须删）；②**可读懂**：面向目标读者（计算机从业者/学员）的表达，术语首次出现给简短解释，逻辑链条完整（现象→原因→结论），读者听一遍能复述核心结论；③**内容正确**：所有技术描述、数据、结论与公网权威来源逐项核对一致，任何一点拿不准即删除或留白，**禁止"看起来合理"的推断性编写**。口播稿写完须对照这三条逐句自检通过后，才进入 Step 2。
3. 按 Step 0 确定的档位撰写口播稿：S 档 250–400 字只讲一个结论、不铺垫；M 档 750–1300 字；L 档按资料原有顺序通读式展开。逐档结构模板见 `references/content-formats.md`。开场钩子取自 `references/hooks-and-cta.md`，结尾自然收尾 + 引导关注/评论的 CTA（**禁止固定结束语模板**，如"以上就是今天所有内容，感谢大家观看，我们下期再见"）。一稿一段、不要频繁换行，可直接复制进 Word。写作规范（口语化、去 AI 味、节奏设计）见 `references/script-writing-guide.md`，必须先读再写。
4. 标题：以作者的初始标题为锚点（如 `Doris读写分离`），在素材消化后打磨为 ≤20 字的发布标题（例：`Doris读写分离：成本直降90%的秘密`）；初始标题与打磨稿一并记录，最终标题在 Step 7 交作者审核确认。
5. 写完后用 `humanizer-zh` 技能过一遍，消除 AI 痕迹；目标是通读下来像人现场讲，不像机器念稿。
6. 产出：创建文件夹 `工作区/<视频标题>/`（文件夹名 = 当前标题，见固定规范第 12 条，不存在则创建），写入 `<视频标题>_口播稿.txt`——**文件内容 = 纯口播正文，顶部不得带标题行**（交付硬门槛 `check_sync.py` 的「字幕==口播稿」比对会把标题行算进正文导致校验失败，2026-09-03 Run23 实测踩坑：多一行标题即 726 vs 715 字符不匹配；标题由文件夹名、PPT 封面页、发布物料承载，不写进稿子）；提取的图片素材放入 `<视频标题>/assets/` 子目录。

#### Step 2：生成视频画面

1. **套模板生成 HTML 幻灯片（强制·不允许自由手写）**：**禁止**每次凭空手写 HTML——必须复制 `templates/slide-template.html`（已含深色科技风主题变量 / 5 大版式组件 / 7 类图表组件 / 内置 CSS 动画类 / 水印&字幕安全区）作为骨架，把 `:root` 的 `--accent`/`--hilite`/`--bg`/`--bg2` 改为该训练营主题色（封面渐变 = `--bg2` → `--accent`，见"训练营主题色"表），再按内容填进各 `<section class="slide">`。每页对应口播稿一个内容段，键盘翻页 + `?page=N` 直达（导出帧用），16:9 画布。页数与停留节奏按档位（S 档 6–10 页、每页 5–8 秒；M 档 12–20 页、每页 10–15 秒；L 档 20–40 页、每页 25–45 秒），S 档画面字号比 L 档大 30%、每页不超过 12 字 + 1 个数字。
2. **专业视觉设计系统（强制·让画面专业不静态）**：每页幻灯片**必须**套用模板里提供的 `templates/slide-template.html` 中的 5 大版式之一（卡片式 `.card-grid` / 双栏 `.split` / 主视觉 `.hero` / 图示 `.diagram-stage` / 数字聚焦 `.stat`），禁止自由堆叠「纯文字 + 单色块」。凡内容涉及**数据、对比、流程、演进、结构**的页，**必须用模板里的图表组件**（`.bars` 柱状 / `table.cmp` 对比表 / SVG 折线 / SVG 环形 / `.stat .num` 大数字 / SVG 架构 / `.timeline` 时间线）呈现，不得写成纯文字段落。关键结论页加 1~2 处模板内置的 CSS 动画类（`.anim-pop` 数字弹入 / `.anim-fade-up` 要点渐入 stagger / `.anim-draw` SVG 线条绘制 / `.anim-fill` 进度填充 / `.anim-pulse` 数字脉冲 / `.anim-highlight` 卡片推进），让画面有"揭示动效"。完整版式/图表/动画/动效纪律见 `references/visual-design.md` §2-§3；静态帧导出时动效终点态作为静态画面保证可读（`animation-fill-mode: forwards`），CSS 动画若需真正进入成片走 `scripts/render_animated.js` 逐帧截屏（见 Step 6 第 2 条）。
3. 深色科技风为主基调；产品/管理/软技能/黄橙系训练营在深色底上配高亮主色即可，不必改用浅色底（保证画面统一）。幻灯片强调色、卡片序号、进度条统一使用该训练营的高亮色（见"训练营主题色"表），图表数据强调也用高亮色、衬托项用中性低饱和色。**排版时为左上角水印预留安全区（约横向 25% × 纵向 15%）**：页面标题、要点、图表标题等关键内容不顶到左上角，左上角区域保持底色/留白，确保后续 Step 4 水印落位后不遮挡任何内容（水印不遮挡规则见 Step 4）。**排版同时为底部字幕预留安全区（约纵向 12%）**：标题、要点、图示主体、卡片、图表等内容不得延伸到画面底部字幕条带区域（约 y 88%~95%），确保 Step 5 字幕落位后不遮挡任何内容（字幕不遮挡规则见固定规范第 15 条）。**页脚不得出现训练营名称/期号等标识**（见固定规范第 14 条），页脚可保留标题、留空或仅放页码。所有图表/图示/卡片必须**对齐网格、间距统一、不重叠、不溢出截断**。
4. 架构图等图示的优先级：**资产中的原始图片 > 官网/官方文档/官方博客的官方架构图（下载或截图，裁切去除无关水印/边框后直接放入画面）> HTML/CSS 重绘**；禁止使用来源不明、无法核实的图片。架构/流程/结构类内容按 `references/visual-design.md` §2.6 / §1.4 的图元与布局规则呈现（进程/库/流/外部依赖图元、分层分区、统一流向）。读书训练营专题：原书关键图示按本流程「忠于原书概念、自绘原创示意图」入画（见读书训练营专项「核心内容入画」规则），**不得直接复制原书图稿像素**，必要时画面/口播标注「示意图，基于《书名》第 N 章」。
5. **图示去侵权元素（强制）**：任何架构图/对比图入画前必须检查并去除图片中可能涉及侵权的标识——第三方平台水印（如 SlideShare/CSDN/知乎/公众号水印）、下载源版权角标、其他作者/机构 Logo、网址域名水印等一律清除，不得带入成片画面。处理优先级：①裁切（水印/Logo 位于图边缘时直接裁掉）；②局部覆盖/模糊（位于内容区且面积小时）；③图像工具按底色修复（如 PIL）；④以上都无法干净去除时**弃用该图**，改用 HTML/CSS 重绘或重新寻找无标识的官方源图。**例外**：讲解某技术/产品本身时，其官方 Logo（如讲 Doris 时图中的 Doris 官方 Logo）属于正常引用可保留，但画面中不得出现与内容无关的第三方品牌推广标识；作者自身标识统一由 Step 4 的左上角水印承担，图中不得重复出现。
6. 用无头浏览器将每页导出为 1080p PNG 帧（见 references/video-production.md 的导出命令）。
7. **图标化表达（强制·丰富 UI、符合美学）**：当前画面以纯文字+色块为主，UI 偏单薄——每页须适度、合理地引入**图标**辅助传达内容，提升信息密度与观感，但必须遵守美学纪律：①统一图标风格（线性/面性一致，统一描边粗细与圆角，不混搭多套图标库）；②图标配色取自该训练营高亮色或中性白/灰，不引入与主题色冲突的杂色；③图标只作辅助——尺寸克制（不超过同行文字高度的 1.4×），置于要点行首/数字旁/概念节点处，不抢主体、不堆砌、不遮挡文字与水印（左上角安全区与底部字幕带仍须留白，见固定规范第 4/15 条）；④每页图标数量适度（一般 1–4 个，关键页可多但须网格对齐、留白均衡），避免满屏图标导致杂乱；⑤图标语义要准——用与内容对应的图标（如「缓存」用层叠/缓存图标、「延迟」用时钟图标、「对比」用双栏/天平图标），禁止无意义装饰图标。图标素材优先取自项目资产 `项目 / 资产 / We-Media`（图标库），无对应资产时再用开源线性图标集（如 Lucide/Feather）自绘，禁止用来源不明、带第三方水印的图标（去侵权见 Step 2 第 5 条）。

#### Step 3：配音

1. **固定音色（强制）**：所有视频统一使用固定音色 `zh-CN-YunxiNeural`（Azure 神经网络语音），不随训练营/项目变化，不依赖任何外部声音模版文件；每次生成视频都使用这一固定音色，禁止自行随意更换。
2. 用 edge-tts 以 `zh-CN-YunxiNeural` 生成配音（`edge-tts --voice zh-CN-YunxiNeural --file <口播稿> --write-media voiceover.mp3 --write-subtitles subs.vtt`）；多音字避坑见 `scripts/gen_sync_subs.py` 说明（禁用 SSML `<phoneme>`，必要时代理同音字）。
3. 拿到配音后核对可用性与时长（`ffprobe`），确认与口播稿内容一致；若配音比口播稿短或长，以配音为准调整口播稿对应段落并说明。
4. 产出：`voiceover.mp3`，记录实际时长（决定帧的停留时间）。
5. **分段配音（与幻灯片一一对应，强制）**：把口播稿按幻灯片切成 N 段（封面 1 段 + 每内容页 1 段），逐段 TTS 各存 clip 后拼接为 `voiceover.mp3`；每段真实时长（秒）写入 `build/segments_durations.json`，作为该片停留时长与字幕时间轴的**唯一来源**（一次调用 `scripts/gen_sync_subs.py` 完成：分段 TTS + 拼接 + 真实字幕 + 时长 JSON）。**禁止**整篇一次性 TTS 后按字数比例切时间轴——画面切换与字幕必须来自分段真实时长。

#### Step 4：水印（仅左上角，全片唯一）

在视频左上角全程叠加 `作者：@Map`，**且全片任何位置只允许这一处作者标识**——封面、正文帧、片尾、角标等处不得再出现作者名/账号信息（包括封面设计元素），避免重复。ffmpeg drawtext 命令见 `references/video-production.md`；也可用 `scripts/video_tool.py watermark` 一键完成。要求：
- 半透明底、不遮挡画面主体、纵向安全区内。
- **排版预留水印安全区（强制）**：画面排版（PPT 幻灯片版式、封面设计、字幕位置）必须为左上角水印预留安全区——左上角区域（约横向 25% × 纵向 15%）不放置任何关键内容：幻灯片标题/要点不顶到左上角、字幕条带避开左上角、封面大标题与徽章整体下移或右移、架构图等图示主体不占满左上角。**排版在前，水印最后落位**，保证水印之下永远是底色/留白，不压任何文字与图形。
- **不遮挡校验（强制）**：水印落位后必须校验是否遮挡内容——按水印区域坐标（x≈0~25%宽，y≈0~15%高）抽帧检查该区域内无字幕、无幻灯片文字、无封面标题、无图示主体；发现遮挡立即调整排版（内容移位/缩小，或水印换到同侧安全位），不得带遮挡交付。

#### Step 5：字幕（与声音逐字一致、与内容对齐，按时间轴出现/消失）

1. **字幕 = 声音 = 内容，三方逐字一致且同频（强制）**：字幕文本与起止时刻必须来自配音真实朗读——用 `scripts/gen_sync_subs.py` 基于 edge-tts 词级 boundary（或 `--write-subtitles` VTT 真实时间轴）逐字产出字幕；真人配音用 faster-whisper 词级时间戳对齐。**禁止**拿口播稿文本直接套时间轴、禁止按字数比例估算字幕时刻。口播稿与配音只要有一字差异（数字读法、语气词、断句、增删），字幕必须跟着配音改，"声音说什么字幕就显示什么"；改稿后必须重配音并同步字幕。
2. **按 cue 时间轴出现与消失**：每条字幕有自己的 start/end，到点才显示、到点即消失，与声音严格同步；**禁止**把某一句字幕固定在画面底部整帧常驻。帧式合成（ffmpeg 无 libass、用 PIL 逐帧绘制字幕）时，同样要按 cue 时间在正确的帧上绘制/擦除，相邻帧的字幕随时间变化、跟随声音切换，而不是一帧一句钉死。
2.1 **长 cue 拆片必须对齐真实发音时刻（强制）**：当一条 cue 因超长需要拆成多片分时显示时，拆片边界的时间**不得按字符宽度比例线性分配**（这是字幕与声音错位的常见根因），必须用 **faster-whisper 词级时间戳（`word_timestamps=True`）对配音做强制对齐**，让每个拆片的 start/end = 该片文字真实发音的起止时刻（参考 `scripts/rebuild_timeline.py`）。拆片后相邻片之间如有小缝隙（<0.35s）或重叠，需平滑处理为单调无重叠序列。成片交付前用 whisper 词流抽帧验证：每个采样点帧上显示的字幕文本，必须与 whisper 该时刻听到的语音逐字对应。
3. **字幕末尾去除标点符号（强制）**：每条字幕（cue）文本**末尾**不得出现任何句读/标点类符号——句号 `。`、逗号 `，`、问号 `？`、感叹号 `！`、顿号 `、`、分号 `；`、冒号 `：`、引号 `""''`、书名号 `《》`、省略号 `……`、破折号 `——`、括号 `）` 等从行末循环删除（删到首个非标点字符为止）；**行内标点保留**（句中逗号、句号、书名号等不影响阅读，可保留）。边界：数字/单位上下文符号（小数点 `2.3`、百分号 `90%`、单位 `℃`、时间码 `00:01:23`）不属句读标点，即使落在行末也保留——例：`74秒到2.3秒。`→`74秒到2.3秒`、`成本直降90%。`→`成本直降90%`，`2.3` 的小数点不在删除集合不会误伤。清洗在字幕渲染层执行：TTS 产出的 VTT/SRT 用 `scripts/video_tool.py sub_clean`（末尾去标点版）清洗再烧录；`scripts/check_sync.py` 的「字幕==口播稿」比对按**去标点后文本**判定（标点不参与逐字一致校验，见交付硬门槛第 270 行）。
4. **单行显示 + 位置固定（强制）**：每条字幕在画面中必须是一行，**禁止折行成多行**；字幕条带固定渲染在画面底部安全区（建议 y 约 88%~95% 高度，不超出该区域）。**字幕不得遮挡任何画面内容（强制）**：幻灯片标题/要点/图示主体、封面标题、卡片文字等都不能被字幕压住——写稿时用短句、避免单条 cue 过长（TTS 的 VTT cue 天然按断句拆分，一般不超一行）；若某条 cue 超出一行宽度：优先按比例缩小字号适配单行（有下限，不得小到看不清），缩到下限仍放不下时，将该句按配音实际停顿拆成两条相邻 cue 分时显示（拆 cue 不改变逐字一致，只让显示时间片变短）。渲染时强制不换行（PIL 绘制不折行、ASS/字幕滤镜不启用自动换行）；合成后抽帧校验字幕条带区域无内容被压住，发现遮挡立即调整（内容上移/缩小或字幕降位），不得带遮挡交付。
5. **重点分级**：普通句用常规白字；数据、结论、核心术语等重点句改用品牌色 + 加粗（甚至放大于 120%），与普通字幕明显区分；可用 ASS 格式实现（模板见 references/video-production.md）。
6. 字幕烧录见 `scripts/video_tool.py subtitle`；无 libass 环境的 PIL 逐帧兜底方案见 references/video-production.md「4. 字幕」。

#### Step 6：封面 + 合成

1. 前 3 秒封面按训练营类型配色与排版：大标题（≤20 字，与开场钩子同一信息点）+ 主视觉 + 一句副标题钩子。**封面背景优先套用项目资产 `项目 / 资产 / We-Media` 中对应训练营的官方封面模板背景**（背景色：架构师=绿、大数据=蓝、产品=紫、AI=红，与其余训练营主题色一致），模板缺失再按主题色表渐变兜底；配色严格取自上方"训练营主题色"表的同色系渐变，不得自行另选颜色。**封面不得出现训练营名称/期号等标识（强制）**——如"架构师训练营""第 1 期"等字样/徽章一律不出现（见固定规范第 14 条）；**封面上也不单独放 `作者：@Map`**——作者标识统一由 Step 4 的左上角水印承担，全片只此一处。
2. **镜头运动层（消除静态感 · 合成层 ffmpeg）**：正文从静态帧合成成片时，**默认改用 `scripts/compose_motion.py`**——对每页静态帧做轻微 **Ken Burns 缓慢推拉摇移（zoompan）** 并用 **xfade 交叉淡入（0.5s）** 衔接，替换原先的「硬切 + 帧静止」，让整支视频"活"起来不再静态。此环节**不改 HTML、不重渲帧、不动配音与字幕时间轴**（帧停留时长仍取 `build/segments_durations.json` 分段真实时长），只换合成方式，因此**四方同频与时长不受影响**。默认 `--preset 1`（轻微运动，正文不晕）；封面段可单独做轻微 zoom-in 提升开场呼吸感。运动强度与节奏选择见 `references/visual-design.md` §3（动效总纲：**运动是消除静态感的主来源，比堆 HTML 动画更有效且更稳**；克制使用页面内动画，不做整体页间转场）。
2.1 **动画帧导出（CSS 动画进视频 · 可选增强）**：当关键页（数字聚焦 / 流程绘制 / 卡片推进）启用了模板里的 CSS 动画类（`.anim-pop`/`.anim-draw`/`.anim-fade-up`/`.anim-fill`/`.anim-highlight`）且需要这些动画**真正进入成片**时，可改用 `scripts/render_animated.js`（Chrome CDP 逐帧截屏）替代「静态帧导出 + compose_motion」——读 `templates/slide-template.html` + `build/segments_durations.json`，对每页做真实时间推进的逐帧截屏（默认 fps=30，CSS 揭示动画 15~30 足够），把动画全程捕获为帧序列后直接 ffmpeg 拼成 body 视频。**Ken Burns 与动画帧导出互斥**——动画帧本身已含动态感，不再叠加 Ken Burns；非动画页仍走 `compose_motion.py`。此增强**默认不启用**，仅在作者明确要求或脚本判断"关键页 CSS 动画值得保留"时触发。脚本依赖 Chrome（`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`，需 `--no-sandbox`）与 node ≥22（内置 WebSocket），ffmpeg 帧序列拼接。用法：`node scripts/render_animated.js <PPT.html> <segments_durations.json> <out_body.mp4> [--fps 30] [--only-page N]`。
3. 用 `scripts/video_tool.py`（或 `compose_motion.py` 合成 body 后）完成整片：封面段 + 正文（每页停留时长取自 `build/segments_durations.json` 的分段真实时长，与配音严格对齐）+ 配音 + 字幕 + 水印 → 成片。成片输出到视频标题文件夹：`<视频标题>/<视频标题>_成片.mp4`（见固定规范第 12 条）。
4. **一律默认 16:9 横屏（强制）**：所有成片默认 1920×1080（16:9）横屏，**不因目标平台自动改变画幅**——即使目标平台是抖音/快手/小红书等竖屏友好平台，默认仍输出 16:9 横屏。仅当作者**明确指定**其他格式（如明确要求 9:16 竖屏）时，才从 16:9 母版派生对应规格（竖屏版 1080×1920 方法：重排 HTML 为竖屏重新导出（首选，字号放大 1.5×）或 16:9 帧居中 + 上下深色延展，派生后字幕与水印位置重新校准）。规格速查见 `references/video-production.md`。

#### Step 7：合规预检 + 作者审核（发布前的强制关卡）

1. （可选·默认略过）如需三平台违禁词官方检测，用 `multi-wordcheck` 技能对口播稿、标题、平台文案做公众号/小红书/抖音检测；该技能纯 API 依赖 `REDFOX_API_KEY`，**无 key 时默认略过，不做强制门禁**。命中即替换后重新生成配音与字幕。作者明确要求执行时才调用。
2. 用 `present_files` 向作者展示成片（默认 16:9 横屏版；作者明确要求其他格式时才含对应规格版本）和口播稿，请作者完整观看审核。
2. **成片必须经作者明确审核通过后，才允许发布到短视频平台**。作者未确认前，只交付成片文件，绝不执行任何发布动作（上传、定时发布、填写文案等均属发布动作）。本关卡为硬门禁，**不适用**固定规范第 16 条的 3 分钟超时续跑——无论等待多久，发布动作都必须等作者明确批准。
3. 作者提出修改意见时，回到对应 Step 修改后重新合成，并再次提交审核；修改只针对作者指出的部分，不擅自扩大改动范围。
4. 作者明确表示"没问题/可以发/确认"之类的批准后，进入 Step 8；发布渠道与账号操作必须由作者本人或其明确授权完成。

#### Step 8：多平台派生 + 发布后复盘

1. 审核通过后，按 `references/multi-platform-repack.md` 派生全部物料：**默认只派生 16:9 横屏成片**（作者明确指定其他格式时才额外派生对应规格，如竖屏版），外加各尺寸封面、小红书图文卡片、公众号文章版 md、各平台标题与文案、话题标签组。派生文案的 `multi-wordcheck` 检测同 Step 7 第 1 条（默认略过）。**切片/切条派生（固定规范第 17 条）**：单个视频总时长 ≤ 30 分钟时不派生切条；> 30 分钟才按段切条。
1.1 **一键发布到微信公众号 + 抖音（发布环节·默认横屏母版）**：作者当前发布渠道为**微信公众号**与**抖音**（固定规范第 18 条）。成片经 Step 7 作者明确审核通过后，支持**一键发布**到这两个渠道——微信公众号：以「视频+标题+正文文案」形式发布（封面用 Step 6 封面、正文用派生的公众号文章版 md）；抖音：以「视频+标题+话题标签」形式发布（母版仍为 16:9 横屏，见固定规范第 13 条）。**一键发布依赖对应平台的开放接口/已授权连接器**：已接入微信公众号素材上传接口、抖音开放平台发布接口或对应连接器时，由本流程调用完成上传；**未接入时（当前状态）不臆造发布动作**，改为产出「发布就绪包」（成片 + 各平台标题/文案/话题标签/封面，集中放视频标题文件夹 `发布/` 子目录）由作者手动发布，并在日志标注「一键发布未接入，已交付发布就绪包」。发布动作始终受 Step 7 硬门禁约束——未经作者明确批准绝不触发。
1.2 **发布后归档百度网盘「自媒体目录」（强制·固定规范第 19 条）**：成片经 Step 7 作者明确批准后，必须把**整个视频标题文件夹**归档到百度网盘 `/自媒体/<训练营名>/<视频标题>/`（9 大类别见固定规范第 19 条）。归档 13 项 = 主交付（成片 / PPT / 口播稿 / 完整工程 ZIP）+ 发布物料（封面 + 抖音/公众号/小红书文案 + 标题标签）+ 工程源（分段稿 / 幻灯片源码 / 字幕 / 配音）。**百度网盘连接器已接入**，按 `references/netdisk-archive.md` 的 SOP 自动执行（连接器无本地二进制直传，完整工程包等二进制走「临时静态托管 → `file_upload_by_url` 转存 → 立即 `unpublish` 下线并 curl 复验 404」），完成后向作者回报网盘落盘路径。归档不阻塞发布、不替代一键发布，是发布完成的归档闭环，凡发布到任意自媒体平台都须触发。**未经批准的成片不得走公网托管跳板上传。**
2. 作者完成发布后，收集平台数据追加一行到 `metrics.csv`（字段：期号/日期/训练营/档位/标题/平台/播放/完播率/点赞/评论/收藏/涨粉/爆款/复盘备注）。
2.1 **体系进度写回（强制）**：把本期覆盖的能力维度 / 主题节点关键词与视频标题写回 `one-hundred-million-syllabus.json` 的 `covered`（开放记录，避免下次重复、保证体系连贯推进）；读书训练营改为更新 `reading_camp_progress.json`。**这是体系化选题的闭环**——不写回会导致下次重复 / 偏离体系主线。
2.2 **分类目录维护（强制 · 每个训练营都要有一份「已生成短视频标题目录」 · 双层维护）**：每产出一条视频（成片进入 Step 7 / Step 8 后），调用 `scripts/update_catalog.py` 把该视频**追加进所属训练营大类的目录索引**，共 9 个大类文件 + 1 个总览，**双层维护**（事实来源 + 项目资产副本）：

   - **① 本地事实来源（自动调用）**：目录落在工作区 `目录/`（随工作区留存，作为事实来源与同步源），9 个大类文件 + 1 个总览：
     - `目录/00_总览.md` —— 9 大类的目录入口（每类累计条数 + 最新一条）
     - `目录/01_架构师训练营.md` … `目录/09_读书训练营.md` —— 单类索引（每行 = 期序｜标题｜档位｜能力维度/主题｜日期）
     - 调用：`python3 scripts/update_catalog.py --camp <大类或别名> --title <视频标题> --tier <S/M/L> --dim <能力维度/主题> [--date <yyyy-mm-dd>] [--workspace <工作区>]`（`--workspace` 缺省 = 当前工作目录）。脚本**幂等**：标题已存在则只刷新信息不重复计数；可手工补录历史视频。
   - **② 项目资产副本（手动场景同步 / 自动场景跳过）**：把本地 `目录/*.md`（10 个）同步到 **tdrive 项目资产**根级 `短视频目录/`（与 `We-Media`、`InfoQ` 平级），让「每个训练营的标题目录」在项目资产里长期留存、跨会话可查。完整 SOP 见 `references/catalog-sync.md`（首次启用需建 `短视频目录/` 并配置 dir_id 到 `scripts/sync_catalog_tdrive.py` 顶部 `TDRIVE_DIR_ID`；同步按 §3 步骤用 tdrive MCP 工具执行，幂等：内容相同则跳过）。触发：作者在场（交互式）→ AI 在 Step 8 末尾询问后执行；cron/定时轮转 → 默认跳过并在日志标注「项目资产同步待手动触发」，下次交互式会话一次性补同步。
   - **用途**：作者日后按大类（如「架构师训练营」）查看已生成过哪些视频、各期讲的是什么能力维度，配合 `one-hundred-million-syllabus.json`（`covered`）掌握体系覆盖进度——这是与 9 大类对齐的「已产出目录资产位」，**两层都要维护好**：本地保证脚本可读可补录，tdrive 保证跨会话长期可查。
3. 按反哺规则更新选题策略（爆款复制、掉速定位、档位校验、选题收敛、平台侧重），供下一期 Step 0 使用；每 10 期做一次小结。

### Quality Gates（交付前自检）

- 开头固定语逐字核对（`大家好，我是Map。`）；结尾为自然收尾 + CTA，**无固定结束语模板**（全文搜索"以上就是今天""下期再见"等固定收尾句，命中即重写）。
- 全文搜索"附件""资料中""文档中""根据原文"等字眼，命中即重写。
- 标题 ≤20 字；时长 ≤ 目标值。
- 水印在每一帧左上角可见，且**全片仅左上角这一处作者标识**（含封面），画面其他位置（封面元素、正文、片尾）无重复作者名/账号信息；前 3 秒封面完整可用。
- **水印不遮挡内容（强制）**：按水印区域坐标（x≈0~25%宽、y≈0~15%高）抽帧检查，字幕、幻灯片文字/标题、封面标题、图示主体均未被水印遮挡；排版已为左上角预留安全区（排版在前、水印最后落位），无任何一帧出现水印压字。
- 所有数字、性能倍数、客户案例与原始资料一致，无编造。
- 通读口播稿一遍，删除排比堆砌、"首先其次最后"式过渡、空洞感叹词等 AI 痕迹。
- **档位自检**：S 档字数 ≤400 且只有一个结论；无铺垫段；字号与页数符合档位规范。
- **钩子与 CTA 自检**：前 3 秒口播与封面大字是同一信息点；结尾自然收尾且带引导关注/评论的 CTA，无固定结束语模板。
- **专业视觉自检（强制·参照 `references/visual-design.md` §6）**：每页套用了专业版式之一（卡片/双栏/主视觉/图示/数字聚焦），无「纯文字+单色块」自由堆叠页；有数据/对比/流程/演进的页已用图/表/图示呈现而非纯文字；图表配色用训练营高亮色+中性色、无杂色；卡片/图元对齐、间距统一、无重叠/溢出截断；页面内容收在顶部 85% 内、未侵入左上角水印区与底部字幕带；静态帧导出状态下每页即使无动画也完整可读。
- **镜头运动自检**：正文合成已采用 Ken Burns 镜头运动 + 交叉淡入（`compose_motion.py`）或明确说明不用运动的原因；无整片全程静止 / 一帧一帧硬切的现象；运动强度默认轻微、未致观感眩晕；帧停留时长仍取自 `segments_durations.json`、四方同频未受影响。
- **发布关卡：作者审核通过前不发布**（本关卡不适用固定规范第 16 条超时续跑）。任何平台发布动作前，必须留有作者明确批准的记录。
- **选题发布价值已判断**：开工前已对作者初始标题做过 7 维发布价值判断（垂直相关性/受众规模/传播钩子/获客漏斗价值/事实可核实性/数据反哺/体系归属）；判为无价值的标题未直接照做，已用绑定技能与连接器检索并产出 3–5 个替代选题、作者确认后才开工；判断结论有记录。
- **选题已体系定位（强制）**：本期选题落在 `references/syllabus.md` 中该训练营的**开放能力地图某维度**上（如「架构师·高并发」「前端·性能」），非游离体系的孤立零散点；开工顺序为「先定体系位置 → 再出题目」；该维度已讲过的主题已换角度 / 新数据或顺延到未覆盖维度；因热点切入的维度已记录。
- **体系进度已写回**：`one-hundred-million-syllabus.json` 的 `covered` 已追加本期能力维度 / 主题节点关键词（开放记录，去重）；读书训练营已更新 `reading_camp_progress.json`。
- **分类目录已维护**：`目录/<所属大类>.md` 已通过 `update_catalog.py` 追加本期视频（期序 / 标题 / 档位 / 能力维度 / 日期）；总览 `目录/00_总览.md` 的累计条数与最新一条已刷新，未重复计数。
- **项目资产目录已同步（手动场景 · 固定规范第 22 条）**：在交互式会话（非 cron）里，AI 已按 `references/catalog-sync.md` 把本地 `目录/*.md`（10 个）同步到 tdrive `短视频目录/`，或作者明确确认「跳过项目资产同步本次」并把决定写入产出日志；cron/定时场景默认跳过并标注「待手动触发」。`短视频目录/` dir_id 首次启用前已配置到 `scripts/sync_catalog_tdrive.py` 顶部 `TDRIVE_DIR_ID`。
- **体系感未违规入画**：封面 / 正文 / 片尾、页脚 / 角标中**无**「第 X 章」「第 X 节」「第 X 期」等编号标识与训练营名称（体系感只体现在内容承接与后台进度，见固定规范第 14 条）。
- **字幕逐字一致（强制）**：抽听 3 处（开头/中段/结尾）核对字幕与配音逐字一致，无多字、漏字、错字（数字、术语尤其核对）；并确认配音内容与口播稿一致（无擅自增删改），即字幕/声音/内容三方对得上。
- **字幕时间同步**：字幕按 cue 时间轴出现/消失、与声音同步切换；不存在整帧固定常驻的字幕。
- **字幕末尾无标点**：每条字幕**末尾**不出现任何标点（句号、逗号、问号等从行末删除，行内标点保留；数字/单位上下文符号如 `2.3`/`90%` 即使末尾也保留）。
- **字幕单行显示**：抽查最长的几条字幕，均为单行完整显示，无折行、无换行、无溢出截断。
- **字幕带与内容互不侵入（强制）**：画面底部字幕带（约 y 85%~98%）专供字幕——抽帧确认该区域内无任何幻灯片内容（标题/要点/图示主体/封面标题/页脚说明均不落入）；同时回溯 HTML 幻灯片源排版，内容主体已收在顶部约 85% 以内、底部 12%~15% 留白专供字幕（见固定规范第 15 条），未与字幕争地；任何一帧出现内容侵入字幕带的，已上移/缩小内容后重新合成，不带遮挡交付。**PIL 管线验证手段**：对 `build/frames/page_*.png` 纯帧（未烧字幕/水印）做像素扫描，确认字幕带 `y∈[918,1000]` 全宽无 `ACCENT/HILITE/PANEL/TEXT` 内容色——`render_frames.py` 的 diagram box 须为 `[x0,y0,宽,高]` 而非两角坐标，否则图示溢出压字幕带。DDIA 三部实测 112 页 0 重叠。
- **画面无训练营标识/期号（强制）**：抽帧检查封面、正文、片尾——画面任何位置（含页脚、角标、封面装饰元素）均无"架构师训练营""第 1 期"等训练营名称/期号/系列名字样；口播稿中亦无此类表述（固定规范第 14 条）。
- **四方同频自动校验（强制·交付硬门槛）**：用 `scripts/check_sync.py` 自动校验——①字幕拼接文本去标点后==口播稿文本去标点后（逐字一致，标点不参与同步判定）；② cue 时间轴单调、无重叠、相邻间隙≤0.35s；③字幕总时长≈配音真实时长（偏差≤1.0s）；④每页停留时长≈该页配音真实时长（偏差≤0.3s）。**exit 0 才允许交付，exit 1 必须重做**——这是字幕=声音=内容=画面四方同频的唯一权威判定，替代任何人工"看起来同步"的感觉判断。脚本依赖 `scripts/gen_sync_subs.py` 产出的真实分段时间轴（禁止整篇一次性 TTS 后按字数比例切分）。
- **PPT 画面全帧禁句/禁标识扫描（强制·双重）**：导出帧前对 `PPT.html` 全文 grep 命中"以上就是今天""下期再见""架构师训练营""第 N 期"等禁句/禁标识，命中即删除对应元素再导帧；成片合成后**再抽帧复核一遍**——封面、正文、片尾任意一帧出现上述内容即视为不合格，必须回炉重做。
- **素材双重来源核验（强制）**：本期素材已同时从项目资产与公网权威渠道（官网/官方文档/官方博客/官方论文/权威技术媒体）获取并核对；资产资料中的关键数据/结论已与官方最新内容校准（版本/数据以官方为准），不存在仅凭资产资料直接成稿的情况；无权威来源支撑的素材未使用。
- **内容质量三达标（强制）**：口播稿逐句审查通过——无一句废话（删后不影响理解与完整性的句子为 0）、读者可读懂（术语有解释、逻辑链条完整）、内容正确（数据/结论与权威来源核对一致，无"看起来合理"的推断性编写）。
- **图示来源可查**：画面中的架构图/对比图必须来自项目资产或官网等官方渠道，能说明来源，禁止使用无法核实来源的图片；读书训练营的图示来源 = 原书对应章节（画面/口播标注出处），按「忠于原书概念、自绘原创示意图」入画，非原书图稿复制。
- **图示无侵权元素**：抽帧检查所有含图示的画面，无第三方平台水印、下载源版权角标、无关第三方 Logo/网址等侵权标识；无法干净去除的图已弃用并重绘。
- **固定音色校验**：成片配音使用固定音色 `zh-CN-YunxiNeural`，未擅自更换其他音色（固定规范第 10 条）。
- **模型与限流降级已处理（强制·固定规范第 23 条）**：开工跑过 `scripts/pick_free_model.py list` + `pick`，本期模型在免费候选内，或已明确标注「免费额度耗尽 → 回退 Auto（计费）」；运行中命中限流信号（`credit 额度已用完` / `使用量超出频率限制` / `429` / `quota exceeded`）已用 `exhausted` 记录（时间 + 原因 + 冷却）并切到下一个可用模型，**未**原地重试同一模型、**未**中断本轮；切换后从断点继续、已完成脚本未重跑；**换模型未降低任何门禁**（`check_sync.py` exit 0、禁句禁标识扫描、字幕带与水印区像素扫描、素材双源核验照跑）；run 日志与 `metrics.csv` 备注列有「模型：<id>｜切换：<from> → <to>｜原因：限流」记录。
- **统一输出目录**：口播稿（txt）、HTML PPT（html）、成片（mp4）均位于以视频标题命名的**同一个文件夹**内，文件夹名与当前标题完全一致（标题终审改名后文件夹已同步重命名）；文件夹内无中间产物混入。
- **一律默认 16:9 横屏（强制）**：成片画幅为 1920×1080（16:9）；**不因目标平台（含抖音/快手/小红书）自动派生竖屏版**；竖屏版仅在作者明确指定格式时作为派生版本产出，且字幕、水印已按竖屏版重新校准。
- **发布后已归档百度网盘「自媒体目录」（强制·固定规范第 19 条）**：成片经作者批准后，**整个视频标题文件夹**已归档到 `/自媒体/<训练营名>/<视频标题>/`，13 项物料（主交付 4 + 发布物料 5 + 工程源 4）齐备、字节数与本地一致；临时托管外链已下线并 curl 复验 404；落盘路径已回报作者。已按 `references/netdisk-archive.md` SOP 执行；未经批准的成片未走公网托管上传。

### Resources

#### references/
- `script-writing-guide.md` — 口播稿写作规范：口语化技巧、AI 味特征清单、训练营分类话术、标题公式。
- `content-formats.md` — **S/M/L 三档内容规范**：字数、结构模板、画面页数与停留节奏、档位与训练营搭配、矩阵分号策略。
- `hooks-and-cta.md` — **前 3 秒钩子库（6 大类 20+ 模板）+ 引导关注/评论 CTA + 期号与系列化 + 视觉锤语言锤**。
- `multi-platform-repack.md` — **一稿多平台派生规范**（抖音/快手/小红书/公众号/YouTube）+ `metrics.csv` 数据反馈闭环与反哺规则。
- `video-production.md` — 视频制作规范：ffmpeg 命令、帧导出、水印/字幕参数、封面设计规范、各平台输出规格。
- `syllabus.md` — **训练营体系 · 开放能力地图（示例维度·章节无穷）**：9 大类各自的专家目标 + 能力维度与示例主题（明确标注「示例，非穷举、非待办清单」）、进度文件 `one-hundred-million-syllabus.json` 字段规范（`covered` 开放列表）。供 Step 0 第 1 条「体系定位」使用。
- `visual-design.md` — **专业视觉设计系统（强制·让画面专业不静态）**：5 大专业版式（卡片/双栏/主视觉/图示/数字聚焦）、图表模板（对比表/柱状/折线/环形/大数字/架构流程/时间线）、动效/镜头运动指引、DO/DON'T 速查与合成前自检清单（§6）。供 Step 2（专业版式与图表）、Step 6（镜头运动消除静态感）使用。解决「视频不好看/画面静态」的规范源头。
- `netdisk-archive.md` — **百度网盘归档 SOP（Step 8 闭环 · 强制）**：`自媒体/<训练营名>/<视频标题>/` 目录规范、13 项归档物料清单与命名、连接器能力边界（无二进制直传/无删除/无文件夹上传）、路径编码避坑（写操作斜杠禁编码、`make_dir` 不可用、`file_list` 需百分号编码且每页 10 条）、`.zip` 被 WAF 403 的绕法、静态托管快照特性、完整 8 步 SOP 与安全边界。
- `catalog-sync.md` — **项目资产目录同步 SOP（Step 8 2.2 · tdrive `短视频目录/` · 强制）**：把本地 `目录/*.md`（10 个）幂等同步到 tdrive 项目资产根级 `短视频目录/`，让每个训练营的标题目录在项目资产里长期留存。包含首次启用建目录 + dir_id 配置、`tdrive.search_file` / `file_download` 比对 / `file_upload`(overwrite) + curl PUT + `file_upload_complete` 完整 SOP、cron 跳过 / 手动场景触发的边界，以及与 `update_catalog.py` 写入器 vs 本 SOP 同步器的解耦说明。
- `model-fallback.md` — **模型使用与限流降级 SOP（固定规范第 23 条 · 强制）**：免费优先 + 限流自动切换。含免费模型实时判定法（本机 `~/.workbuddy/cache/acc-product-config-v3.json` 的 `models[].credits`，`x0.00` = 免费，清单**禁止硬编码**）、限流信号识别（`credit 额度已用完` / `使用量超出频率限制` / `429` / `quota exceeded`）、标准动作（标记冷却 → 取下一个 → 断点续跑 → 门禁不减 → 记录）、三种运行形态的切换执行方式（CLI `/model` / 桌面端下拉 / 无人值守靠状态文件跨轮生效）、避坑与自检清单。

#### scripts/
- `video_tool.py` — ffmpeg 封装：`frames`（帧转视频）、`watermark`（叠加水印）、`subtitle`（烧录 SRT/ASS）、`cover`（前 3 秒封面拼接）、`audio`（合成音轨）、`sub_clean`（去除 VTT/SRT 每条字幕**末尾**的所有句读/标点类符号，保留行内标点和数字/单位上下文符号如小数点、百分号）。依赖 ffmpeg，执行 `python3 video_tool.py <subcommand> --help` 查看用法。
- `analyze_voice.py` — 配音音色基线分析（自相关法算基频 F0 推断性别/音高 + faster-whisper 转写算语速）。依赖 numpy（必需）、ffmpeg、faster-whisper（可选，缺则只出 F0）。用法：`python3 analyze_voice.py 参考.mp3 候选.mp3` 并排输出对比。固定音色（`zh-CN-YunxiNeural`）下该脚本仅供诊断/调参，不再是强制门禁。
- `compose_motion.py` — **镜头运动层合成（消除静态感，Step 6 推荐默认）**：读静态帧目录 + `segments_durations.json`，逐帧做轻微 Ken Burns 缓慢推拉摇移（zoompan）+ xfade 0.5s 交叉淡入衔接，替换「硬切 + 帧静止」。不改 HTML/不重渲帧/不动配音字幕时间轴，只换合成方式，四方同频与时长不受影响。用法：`python3 compose_motion.py <frames_dir> <durations.json> <out.mp4> [--fps 30] [--preset 1|2|3]`（preset 1=轻微推荐正文）。运动/动画纪律见 `references/visual-design.md` §3。
- `render_animated.js` — **动画帧导出（CSS 动画进视频 · Step 6 可选增强）**：用 Chrome DevTools Protocol(CDP) 对每页 HTML 做真实时间推进的逐帧截屏，把模板里的 CSS 动画类（`.anim-pop` 数字弹入 / `.anim-draw` 线条绘制 / `.anim-fade-up` 要点渐入 / `.anim-fill` 进度填充）真正捕获进帧序列，再 ffmpeg 拼成 body 视频——替代「每页一张静态帧」的导出方式，让动画全程进入成片。**与 `compose_motion.py` 互斥**（Ken Burns 与动画帧二选一，非动画页仍走 Ken Burns）。依赖 Chrome（`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`，需 `--no-sandbox`）+ node ≥22（内置 WebSocket/fetch）+ ffmpeg。用法：`node scripts/render_animated.js <PPT.html> <segments_durations.json> <out_body.mp4> [--fps 30] [--only-page N]`。详见 Step 6 第 2.1 条。
- `pick_free_model.py` — **免费模型挑选与限流降级（固定规范第 23 条配套 · 强制）**：实时读本机产品配置（`~/.workbuddy/cache/acc-product-config-v3.json`，回退 app 内置 `product*.json`，可用 `ONE_HUNDRED_MILLION_MODEL_CONFIG` 覆盖），以 `credits` 字段判定免费模型（`x0.00` = 免费、`null` 不选），维护工作区状态文件 `one-hundred-million-model-fallback.json`。子命令：`list`（列免费模型 + 冷却状态）、`pick`（stdout 输出下一个可用模型 id，exit 0；免费全耗尽输出 `AUTO` 且 exit 2；读不到配置 exit 3）、`exhausted <id> [--reason S] [--cooldown N]`（标记限流，默认冷却 7200s）、`reset [--id X]`（释放）、`current <id>`（记录当前模型）。用法：`python3 pick_free_model.py <cmd> [--workspace <工作区>]`。完整 SOP 见 `references/model-fallback.md`。
- `update_catalog.py` — **分类目录维护（Step 8 2.2 强制 · 本地事实来源）**：把一条已产出视频幂等追加进所属训练营大类的目录索引（**本地** `目录/` —— 事实来源；项目资产副本由 `references/catalog-sync.md` 同步到 tdrive `短视频目录/`，与本脚本解耦），9 大类文件 + `00_总览.md`。用法：`python3 update_catalog.py --camp <大类或别名> --title <视频标题> --tier <S/M/L> --dim <能力维度/主题> [--date yyyy-mm-dd] [--workspace <工作区>]`。幂等：标题已存在则只刷新不重复计数；支持别名归入 9 大类（不匹配则报错退出）。
- `sync_catalog_tdrive.py` — **项目资产目录同步辅助器（Step 8 2.2 · tdrive `短视频目录/` · 与 `update_catalog.py` 解耦）**：`update_catalog.py` 写本地 `目录/`（事实来源）后，本脚本持有 tdrive 目标 dir_id 配置（TDRIVE_DIR_ID 常量 / 环境变量 ONE_HUNDRED_MILLION_CATALOG_DIR_ID / 配置文件 `scripts/.tdrive_dir_id`），提供 `check`（校验配置 + 列待同步文件）、`list`（路径/size/sha1）、`set-dir-id <id>`（首次启用写入 dir_id）、`sync`（打印 `references/catalog-sync.md` §3 SOP 步骤）。**不直接执行 tdrive 写入**——Project Drive mutating 由 AI 按 SOP 用 tdrive MCP 工具按序执行（交互式经作者确认，cron 跳过），符合 project-file-rules。首次启用前须在 tdrive 项目资产根目录建 `短视频目录/` 并把 dir_id 配置进来。
- `gen_sync_subs.py` — **分段真实时间轴生成（四方同频的核心）**：读 `segments.txt`（每页一行，首行封面），逐段 edge-tts（开启句级 SentenceBoundary）拿到真实起止时刻，concat 成 `voiceover.mp3`，写 `subtitles.srt`（句级真实时间轴，句内按字符比例拆单行≤32字、去尾句号）与 `segments_durations.json`（`{durations,starts,ends,total}` 作为画面停留与字幕时间的唯一来源）。含全局去重叠+最小 0.15s 兜底。用法：`python3 gen_sync_subs.py <segments.txt> <out_dir> <voice> [cover_min]`。**多音字避坑（已踩·物化视图项目验证）**：edge-tts 在 `zh-CN-YunxiNeural` 上对中文多音字按自身语境猜读，可能偏离用户意图。**禁用 SSML `<phoneme>` 强制读音**——该音色下 `<phoneme>` 会让音频膨胀 4~5 倍（同句 8s→36s），总时长失控不可用。正确做法（仅当 edge-tts 读错方向时）：**代理同音字**——TTS 输入把易错字临时换成一个 edge-tts 必读正确的同音字，句边界文本再换回原字（字幕仍显示正确汉字、观众听到正确读音）；须逐段校验「替换后句边界文本拼接 == 原口播稿」。**物化视图项目特例**：数据库语境「行」(一行数据=row) 用户确认读 **háng**，而 edge-tts 默认正是读 háng，故该项目无需代理，直接喂原始口播稿即可（见 `revert_hang.py`：取消 行→形 替换、还原原始文本；它是 `fix_xing.py` 的反向）。
- `check_sync.py` — **四方同频自动校验（交付硬门槛）**：校验字幕==口播稿、cue 单调无重叠、总时长≈配音、每页≈分段时长，exit 0 才交付。用法：`python3 check_sync.py <segments_durations.json> <subs.srt> <script.txt> <voiceover.mp3>`。
- `render_frames.py` — PIL 渲染每页 1920×1080 帧（读书训练营自绘示意图管线）：读 `pages_data.py` 的 PAGES/DIAGRAMS/TITLE，输出 `build/frames/page_NN.png`。**坑（已踩）**：diagram box 必须传 `[x0, y0, 宽, 高]`，**勿传两角坐标 `[x0,y0,x1,y1]`**——所有 `draw_*` 按 `(x0,y0,w,h)` 解包，传两角坐标会把 `x1`/`y1` 当宽高，导致图示从 `y0` 向下画到 `y0+y1`（≈1338，裁到 1080）**溢出压入字幕带（y=928）**。正确值 `[100,470,W-200,398]`，图示底部收在 y≈868，与字幕带（918+）留白。用法：`python3 render_frames.py <build_dir>`。
- `rebuild_timeline.py` — 字幕时间轴精确对齐（Step 5 第 2.1 条配套）：对配音用 faster-whisper 词级时间戳做强制对齐，把长 cue 拆片后的每个子片映射到真实发音起止时刻，解决按字符宽度线性分配时间导致的字幕与声音错位。依赖 faster-whisper 词级 JSON。用法：`python3 rebuild_timeline.py <subs_clean.vtt> <whisper_words.json> <out.vtt> [max_w]`。
- 配音音色固定为 `zh-CN-YunxiNeural`（Step 3 统一使用，不依赖任何外部模版文件）；品牌字体等留空待作者放入。

#### templates/
- `slide-template.html` — **HTML 幻灯片模板（Step 2 套模板强制使用）**：深色科技风 16:9 骨架，含 CSS 主题变量（按训练营主色替换 `--accent`/`--hilite`/`--bg`/`--bg2`）、5 大版式组件（`.card-grid` 卡片 / `.split` 双栏 / `.hero` 主视觉 / `.diagram-stage` 图示 / `.stat` 数字聚焦）、7 类图表组件（`.bars` 柱状 / `table.cmp` 对比表 / SVG 折线 / SVG 环形 / `.stat .num` 大数字 / SVG 架构 / `.timeline` 时间线）、内置 CSS 动画类（`.anim-pop` 数字弹入 / `.anim-fade-up` 要点渐入 + `.d1`~`.d4` stagger / `.anim-highlight` 卡片推进 / `.anim-draw` SVG 线条绘制 / `.anim-fill` 进度填充 / `.anim-pulse` 数字脉冲）、水印安全区（`.watermark-zone`）+ 字幕带安全区（`.subtitle-zone`）提示块、`?page=N` 直达 + 键盘翻页。**禁止自由手写 HTML**——Step 2 必须复制本模板改主题色与内容；`animation-fill-mode: forwards` 保证静态帧导出时动效终点态可读；CSS 动画若需进入成片走 `scripts/render_animated.js` 逐帧截屏（见 Step 6 第 2.1 条）。
